GBL・ロケット団・対戦記録・模擬戦で壊してはいけない決まり（詳細は .claude/rules/gbl.md）
1. 一覧のマスとタップして開く1対1の結果を食い違わせない（シールド枚数・ブラフ・わざ・SIMOPT・listDefaults をそろえる）
2. pvp-engine.js を変えたら engine-test 21項目すべて緑（保存前に自動で確認される）
3. 同じターンの順番（2026-09-22確定）: SPを入力したターンに当たるノーマルが先／そのノーマルで倒れてしまうならSPが先でそのノーマルは入らない（相打ちにならない）／打ちかけの途中ならSPが先。バトルルールの一覧は .claude/drafts/gbl-battle-rules-draft.md
4. AIはずるをしない（未登場の控え・ユーザーの選択を見ない）。例外はHARDのomniだけ
5. 下読みは決断のたびにその瞬間の状態で（シールドは shAt、キャッシュの鍵に shSig）
6. 模擬戦の答えは使う直前に ansFits で照合する。切れ目は cutAt（ターン数の倍数で判定しない）
7. タイムラインは sortTimeline（gt→li→種別）。updateHud には必ずその行の li を渡す。autoScroll は行を出した後
8. メガ・ゲンシは1匹まで（megaOver）。★登録リストはじぶん／あいての側を必ず渡す
9. 保存データは Array.isArray などで形を確かめる。起動時の処理は bootStep の中に置く
10. 起動に要るファイルを足したら gbl・rocket・battlelog の sw.js の一覧と CACHE 名を更新
11. ロケット団: あいてはシャドウ固定・特別わざなし・わざは最悪ケース基準・SP待ち時間は RK_SP_TURNS
12. 表記: 「ブラフ」（釣りと書かない）・「殴る」「子」と書かない・シャドウは名前の左のマーク・点灯に光彩を付けない
13. 見た目を変えたら375px・ダーク/ライトで確認。模擬戦はバトル中の全画面表示でも撮る
14. バトルルールの検査 rules-scn（期待値 .claude/checks/rules/expected/）・rules-random（種固定600戦）が保存前に流れる。期待値の書き直しはタダシさんの確認のあとだけ
15. 模擬戦の⏩決断まで: 威力調整の手前で止める（revealTo(stop, true)）・決着に達したら⏸中でも atStop（fxIdle で演出の列の最後に）。演出の列（FX_Q）は再生済みの行を飛ばす。濃紺で固定の面（.rbwin・.rbhud）は文字色も固定
