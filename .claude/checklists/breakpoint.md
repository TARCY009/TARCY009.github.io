GBLブレイクポイントで壊してはいけない決まり（詳細は .claude/rules/breakpoint.md）
1. 1対1シミュのダメージと必ず一致させる（pvp-engine.js の buildStats／damage を使う）
2. SPアタックは対象外
3. 交換できないポケモンは最低個体値10・PLの下限あり。下限は ivFloorOf(key, shadow)（シャドウも渡す）
4. META_LISTS には meta_moves.js の確定値を当ててから使う
5. 画面の状態をURLに書き戻さない
6. CP制限は順位（SCP順）が軸。おすすめ個体は勝ち数の順
7. 勝ち数のキャッシュの鍵には前提（固定した相手・あいての順位の範囲）を含める
8. 対戦データのSPの消費ゲージは e、ノーマルは eg と tn（取り違えない）
9. 入力欄は16px・入力中の欄には書き戻さない
