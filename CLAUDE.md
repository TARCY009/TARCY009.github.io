# TARCY009.github.io 開発メモ（Claude Code用）

ポケモンGO攻略ツール集。公開先: https://gonavi.jp/（2026-09-12に独自ドメインへ移行。旧 https://tarcy009.github.io/ は自動で転送される）

### 決まりのメモの置き場所と見張り役（2026-09-17新設・タダシさん承認・恒久ルール）

このファイルが大きくなりすぎて「Prompt is too long」が出たため、**ツールごとの決まりを `.claude/rules/` へ分けている**。
分けたメモは、対象のファイルを Read で開いた瞬間に Claude Code が自動で読み込む（`paths:` の指定）。

| メモ | 対象 | 読み込まれるとき |
|---|---|---|
| この CLAUDE.md | 全ツール共通の決まり・トップページ・共通ファイル（snap.js・home.js など）・データ更新・複数ツールにまたがる節 | 毎回 |
| `.claude/rules/gbl.md` | GBL・ロケット団・対戦記録・模擬戦・対戦エンジン | `assets/gbl-app.js`・`assets/pvp-engine.js`・`gbl/`・`rocket/`・`battlelog/`・`pvp-tests/` などを開いたとき |
| `.claude/rules/raid.md` | レイド火力チェッカー・レイドシミュレーター・タイプ別火力 | `template.html`・`dps/`・`raid/`・`assets/raid-engine.js`・`type-dps/` |
| `.claude/rules/iv.md` | 個体値チェッカー | `iv-checker/` |
| `.claude/rules/gym.md` | ジム挑戦・ジム防衛・マックスバトル対策 | `gym-attack/`・`gym-defense/`・`max-battle/` |
| `.claude/rules/breakpoint.md` | GBLブレイクポイント | `breakpoint/` |
| `.claude/rules/dex.md` | ステータス図鑑・わざ図鑑・耐久指数・マックスバトル タイプ別 | `pokedex/`・`move-dex/`・`bulk/`・`max-type/` |

**⚠ 共通ファイル（`build_data.py`・`assets/snap.js` など）を直して、分けたツールの結果が変わりうるときは、影響するツールのファイルを Read で開いてそのツールの決まりも読むこと**（見張り役は共通ファイルの書き換えでは止めないため）。

**見張り役（`.claude/hooks/guard.py`・`.claude/settings.json` で登録）**:
1. **書き換える前**: 対象のメモがこのチャットで読み込まれていなければ止める（エディタでもコマンドでも）。止められたら、対象のファイルを Read で開いて決まりを読んでからやり直す
2. **書き換えた直後**: そのツールの「壊してはいけない決まり」の短い一覧（`.claude/checklists/`）を見せる
3. **保存（git commit）の前**: 保存するファイルのメモがすべて読み込まれているか確かめる
4. **保存の前**: 答え合わせ（`.claude/checks/run.py`）を流し、不合格なら止める（画面なし専用ブラウザで1つ1秒ほど）。
   いまあるもの: **GBL**＝engine-test 21項目（ブレイクポイントも同じもの）／**個体値チェッカー**＝CP・順位・逆引き・進化後CPの16項目／
   **レイド**＝スクショ5例のボスの攻撃時刻15個＋回帰5通り（`raid-baseline.json`）／
   **マックスバトル**＝アタッカーが「ダメージ最大のマックスわざ」を選ぶか 5項目（`max-attacker`・全ボス×ダイマックス勢 約9.8万組）。
   **出口**＝各ツールの出口（下の「1ポケモン1ページ用の出口」）の数字が画面と同じか（`exit-rank` 20項目＝タイプ別火力・耐久指数・ジム防衛・マックスバトル タイプ別／`exit-gbl` 16項目＝GBLの環境一覧・ロケット団のランキング）。
   **ジム挑戦・図鑑はまだ無い**。
   **わざと仕様を変えて答えが変わるときは、答えを書き換える前に必ずタダシさんに確認する**（結果に合わせて答えを勝手に直さない）

- 会話が要約されると「読み込んだ」印は消え、もう一度読ませる
- 対応表は `.claude/hooks/rulesets.json`。**メモの `paths:` と対応表の `paths` は必ず同じにする**（チャット開始時に自己点検が食い違いを知らせる）
- **新しくツールの決まりを分けるとき**: ①CLAUDE.md の節を1行も変えずに移す（移す前後で見出し・⚠の数と行の中身を機械で照合する） ②rulesets.json に追加 ③checklists を作る ④答え合わせがあれば checks/run.py に足す
- 記録の書き方: そのツールの決まりは**そのツールのメモ**に書く。全ツールに効く決まりはこの CLAUDE.md に書く
- **メモには決まりだけを書く**（2026-09-17・タダシさん承認）。経緯の物語・検証の件数や速度・日ごとの作業まとめは書かない（残したいときは `.claude/archive/` へ。ここは自動では読み込まれない）。決まりの理由は再発防止に要る1句だけ。「不採用」「廃止」は1行で理由つき。答え合わせの基準値は残す
- **2026-09-17に CLAUDE.md と gbl.md を縮めた**（経緯・検証の記録を削除）。**縮める前の全文は `.claude/archive/CLAUDE-2026-09-17-full.md`・`.claude/archive/gbl-2026-09-17-full.md`**。昔の経緯や検証の数値が要るときはそこを読む
- `.claude/` は公開サイトには配信されない（点で始まるフォルダは除外される。`.github/` で404を確認済み）。`settings.local.json`・`launch.json`・`state/` はgitに入れない
- 答え合わせのブラウザは `~/Library/Caches/ms-playwright/chromium_headless_shell-*`。通常の Chrome は画面なしモードで終わらないことがある（消えていたら不合格扱いで止まる）


### 各ツールのタイトル直下の紹介文（2026-09-22・タダシさん指示・アドセンス「有用性の低いコンテンツ」への手当て・恒久）

**全ツールのタイトルのすぐ下に、目に見える3文の紹介文**（何のツールで・どう使い・何が分かるか）。📖説明モードに隠さない。**審査後も消さない**。

- 置き場所は **`<header>` の中・h1（添え書き `.lead`/`small` があればその後ろ）の直後**の `<p class="intro">`。見た目は `assets/pageheader.css` の `header .intro` 1か所（`--sub` の色・.82rem・左そろえ）。広告の枠A（header の直後）の並びは変えない
- **GBL・ロケット団・対戦記録は `assets/gbl-app.js` の骨組みの `#intro`**（ロケット団・対戦記録は eyebrow と同じ場所で `textContent` を差し替える）。**レイド火力は template.html と dps/index.html の両方**
- **ツールの機能を変えたら紹介文も見直す**（例: マックスバトルのランクは自動判定なので「★5・★6の切り替え」とは書かない）。新しいツールにも必ず付ける

### 独自ドメイン gonavi.jp（2026-09-12取得・タダシさん）

- **お名前.comで取得**（1年目0円・翌年から年3,124円・カード自動更新ON・期限2027/09/30・Whois公開代行つき・有料オプションなし）
- **ネームサーバーはお名前.com（01〜04.dnsv.jp）**。DNSは5行:
  A `gonavi.jp` → 185.199.108.153／109.153／110.153／111.153（GitHub Pages）・CNAME `www.gonavi.jp` → `tarcy009.github.io`
- **リポジトリ直下の `CNAME` ファイル（中身 `gonavi.jp`）で独自ドメインを指定**。**⚠ 消すと旧アドレスに戻り、gonavi.jp で開けなくなる**
- 絶対アドレス（canonical・og:url・og:image・運営者情報のURL）は `https://gonavi.jp/`。**新しいページでも gonavi.jp で書く**
- **localStorage はアドレスごとに別**（旧アドレスの記録は「データの引っ越し」ページで移す）
構成: **`/`(トップページ＝ツール一覧)** `/dps/`(レイド火力チェッカー) `/raid/` `/type-dps/` `/max-battle/` `/gym-attack/` `/gym-defense/` `/gbl/` `/rocket/` `/battlelog/` `/breakpoint/` `/iv-checker/` `/pokedex/` `/bulk/`(耐久指数ランキング) `/move-dex/`(わざ図鑑) `/max-type/`(マックスバトル タイプ別アタッカー) `/max-type/tank/`(同 タンク)

### トップページと引っ越し（2026-09-01確定・タダシさん指示）

**レイド火力チェッカーは `/dps/`、`/` はトップページ（ツール一覧）**。

- **`build_data.py` の出力先は `dps/index.html`**（`template.html` はルートのまま）
- **`/dps/manifest.webmanifest`**（start_url/scope を `/dps/`）と、トップページ用の **`/manifest.webmanifest`**
  （`GOツール`・アイコンは `assets/icons/home/`）を別々に持つ
- template.html に OGP と meta description あり

**トップページ（`index.html`）**: 手書きの静的ページ（生成物ではない）。ツールの**タイル**を**3グループ**に分ける:
**レイド・ジム**(dps/raid/type-dps/max-battle/gym-attack/gym-defense)・
**対戦**(gbl/battlelog/breakpoint/rocket・**見出しは「Trainer Battle ／ トレーナーバトル」**（2026-09-08）。「バトルリーグ」「対戦」とは書かない。meta descriptionも同じ)・**調べる**(iv-checker/pokedex)。
グループ色は レイド=赤〜オレンジ／対戦=水色／調べる=紫。**カードのアイコンは各ツールの実物**（`assets/icons/<名前>/icon-192.png`）。
**loading=lazy は使わない**（12枚175KBで、遅延だと「画像が出ない」に見える）

### トップページの雰囲気は「大人向け・落ち着いたラグジュアリー」（2026-09-02確定・タダシさん指示）

**色は完成しているので変えない**。作り込むのは**表記・配置・字づかい**。**決めごとは4つ**:

1. **絵文字を使わない**（安っぽい）。グループ見出しは**英字の小見出し＋和文＋1pxの罫線＋2桁の数**（`Raid & Gym ／ レイド・ジム ──────── 06`）
2. **話し言葉を使わない**。体言止めの短い語（「速く倒す方法を探す」→「短時間で倒す構成」）
3. **ロゴマークをサイト名の左に置かない**（"GOGOナビ"に見える）。**上の細いバーの左端**に独立、右端にテーマ・説明の切り替え。
   その左に**「読み物」への細い罫線のピル**（`.tblink`・2026-09-22タダシさん指示・審査の準備。審査後に外してもよい）
4. **動く飾りを足さない**（光の帯のアニメは廃止）。効かせるのは**余白・字間・1pxの罫線・ごく淡いグラデーション**だけ

- **サイト名の書体は「禅角ゴシック New」900**（6案から選択・ツール内がゴシック系のため）。**明朝はもう使わない**
  - 字間 **.1em**（スマホ .08em。極太は広げるとバラける）。銀色のグラデーション＋下に**金の細い罫線**
  - 取り寄せは `text=GOナビ` の**4文字だけ**。失敗時はヒラギノ角ゴ900
  - **色は金属のブルーシルバー**（2026-09-03選択）。**真ん中（49〜53%）の急な明暗＝「反射の折れ目」**が金属らしさ。ライトは同じ位置で濃紺〜青
  - **⚠ ライトは明るさを持ち上げて確定**（2026-09-04。濃紺〜ほぼ黒の `#18243d`・`#141f34` は明るい背景 `#e9f1ff`〜`#fdeee6` と釣り合わない）。
    **白を混ぜて明るさだけ上げる**方式で、折れ目・押し出し・落ち影の6色＋輪郭色を同じ配合率で明るくし、影の強さも弱める（濃い影が残ると文字だけ浮く）
  - **影は「板の厚み」**: **押し出し4段**＋深い落ち影
  - **「GO」だけ輪郭+1.3px・横に4%伸ばす**（4%＝縦長の感じだけが消える量）
  - **「ナ」と「ビ」の間だけ`margin-left`で-.26em**（2026-09-08確定。字形の余白の差でこの1か所だけ広く見えるため）
  - **⚠ 輪郭は塗りの「下」**（`paint-order:stroke fill`。上だと内側へ食い込み筒に見える）
  - **⚠ `transform` をかけた部分には親の `background-clip:text` の塗りが届かない** → **変形する要素にも同じ塗りを持たせる**

### トップページの「GO」のOに方位磁針の針（2026-09-04・タダシさん指示）

**看板のOの穴にもロゴと同じ針**: マゼンタ`#ff5abe`の針＋青灰`#8c98c6`の後ろ半分＋白い中心、右上向き（SVG `rotate(38 50 50)`）。
部品は `index.html` の `h1 .o`（Oを包む inline-block・**自分の塗りを持たせる**）＋`h1 .ndl`（絶対配置のSVG・幅高さ.46em・`pointer-events:none`）。

- 禅角900の「O」の穴: 横0.17〜0.55em（中心0.36em）・基線から0.12〜0.59em（中心0.355em）。箱は幅＝字送り0.726em＋字間0.1em、
  高さ1.12em、基線は上から0.94em（CSSの初期位置 **`left:calc(50% - .05em)`・`top:calc(50% + .025em)`**）
- **⚠ 針は h1 の「外」に置く**（h1 の7段 drop-shadow フィルター＋clip:text の中の SVG は iPhone Safari でクラッシュしうる）。
  h1 を `.wmbox`（inline-block・position:relative）で包み、`.ndl` はその中の絶対配置。位置は JS（`placeNeedle`）が「O」の矩形から計算。
  **フィルター付き・clip:text の要素の中に、位置指定の要素や SVG を入れない**（恒久）
  - **書体が届いたあとに必ず計算し直す**: `document.fonts.ready`／`loadingdone`／`load`／`resize`／`pageshow`／`ResizeObserver(h1)`＋後追い（0.3〜5秒）で `place()`（何度呼んでも害なし）
  - **⚠ 穴の位置を決め打ちしない**（禅角の値のままだと代替のヒラギノ（穴は左0.45em・0.39em上）で縁に付く）。
    `place()` が**いま出ている書体の「O」を canvas に描いて穴を走査**（`holeOf`）し中心に置く。基線は O の直後の**高さ0の印 `h1 .bp`** から取る。
    `.go` の横1.04倍は `DOMMatrix` で読む。走査できない環境だけ禅角の値。基準値: 禅角=0.377em／ヒラギノ=0.468em
  - **⚠ ローカルのブラウザパネルが非表示だと `ResizeObserver` が発火しない** → `window.dispatchEvent(new Event('resize'))` で計算し直してから測る

### 説明ボタンの絵文字は📖（2026-09-04・タダシさん指示）

**💡は明るさ設定と間違える**ので説明ありモードのボタンは**📖**（`explain.js` ほか置換済み）。
**このメモの過去の記述の「💡説明モード」「💡ON」はすべて📖のこと**。

### 全ツールのタイトル文字（2026-09-04確定・完成）

**ダーク＝`.wmk`はツールごとの明るいベタ色（ライトと同じ色相）・`.wmksuf`は常に白／ライト＝そのツールの色のベタ塗り**。
**金属加工・グラデーション・押し出し・落ち影は一切なし**（細い字では暗い部分が線を占め全ツール同じ黒っぽい色に見える。ダークはタダシさん案「白＋色を半分ずつ」で確定）。
書体は全ツール**Noto Sans JP 900**。定義は **`assets/wordmark.css` 1か所**（`build_wordmark.py`の出力・手で編集しない）。

ページ側（新ツールでも同じ）:

1. `<head>` に `<link rel="stylesheet" href="/assets/wordmark.css">`（explain.css の直後）
2. ツール名の要素に **`class="wmk" data-tool="<キー>"`**。決まり文句の部分は **`class="wmksuf"`** の別要素で白
   （例: `<span class="wmk" data-tool="gym-defense">ジム防衛</span><span class="wmksuf">オススメツール</span>`）。
   自動の対象は `build_wordmark.py`の`SUFFIXES`の語尾だけ。当てはまらないタイトルは手で分割する

**教訓（色調整）**: ①**hexの数値でなく**、他の文字と並べたスクショで判断 ②**「やめてほしいこと」と「残してほしいこと」**の両方を読む
③微妙な差の比較より**具体的でシンプルな案を早めに出す** ④**「戻して」は「直前の値」か「機能ごと変更前」か**を確認 ⑤**ユーザーの具体的な代替案はすぐ採用してよい**

**白/色の配分（現在）**:

| ツール | 色（`.wmk`） | 白（`.wmksuf`） |
|---|---|---|
| dps | レイド火力 | チェッカー |
| raid | レイド | シミュレーター |
| type-dps | タイプ別火力 | レイド（前）・ランキング（後） |
| iv-checker | 個体値 | チェッカー |
| pokedex | 図鑑 | ステータス |
| gbl | シミュレーター | GBL |
| rocket | `対策シミュレーター` | `GOロケット団` |
| battlelog | 対戦記録 | GBL |
| breakpoint | ブレイクポイント | GBL |
| gym-defense | ジム防衛 | オススメツール |

- **タイプ別火力は先頭に白で「レイド」**（2026-09-20タダシさん指示・マックスバトル タイプ別の「マックスバトル」と同じ形）。ツール名そのものは変えていないので、manifest・`<title>`・トップページのカードは「タイプ別火力ランキング」のまま
- **GBL関連4ツールは「ブランド接頭辞=白・機能を表す語=色」**
- breakpoint は前が決まり文句（PREFIX型）。`.wmksuf`に入れ、`white-space:nowrap`の外枠で折り返しを防ぐ（元は`h1 b{white-space:nowrap}`の塊）。
  **🎯の絵文字は2026-09-20にタダシさん指示で削除**（ボールマークと並ぶと飾りが多いため）
- **⚠ 個体値チェッカーの副題 `<small>` は`.wmk`/`.wmksuf`の外（h1直下）**（中だと`.wmk small{color:inherit}`で`color`が付く。外なら`h1 small{color:var(--sub)}`）

**⚠ eyebrow・タイトル・白の3色構成（全ツールの恒久ルール・基準はマックスバトル）**:

> **eyebrow（英字の小見出し `.eyebrow`）＝そのツールの色を暗くした濃い色／タイトルの固有名部分＝`.wmk`の明るい色（変更なし）／
> 決まり文句・ブランド接頭辞（`.wmksuf`）＝白**

- **`.wmk`の明るい色（`wordmark.css`の`--wmk-d`）は変えない**（他でも共有）。**eyebrowの色はページのローカルCSS**で、HLSのL値を0.14下げる
  （max-battle: タイトルL0.73→eyebrowL0.62・彩度そのまま）。pokedexのeyebrowは`#914cff`（タイトルは`.wmk[data-tool="pokedex"]`の`#bd93ff`）
- eyebrowの文言: dps `<div class="eyebrow">Pok&eacute;mon GO Raid DPS</div>`／raid `<div class="eyebrow">Pok&eacute;mon GO Raid Simulator</div>`（`<h1>`の直前）／
  type-dps「Pokémon GO Type DPS Ranking」／gym-attack・gym-defense「Pokémon GO Gym Battle Attack/Defense」／pokedex「Pok&eacute;mon GO Status Dex」／
  gbl・rocket・battlelog・breakpoint「Pokémon GO Battle League」「Pokémon GO Team Rocket」「Pokémon GO Battle Log」「Pokémon GO Breakpoint」
- 書体は`Oswald`（`<head>`に`family=Oswald:wght@600`）。eyebrowがブランド表記を兼ねるので「ポケモンGO」の添え書きは置かない
  （廃止: `<span class="brand">ポケモンGO</span>`・type-dpsの`<small>ポケモンGO</small>`・pokedexの`<span>ポケモンGO</span>`）
- **max-battleは基準なので無変更**。**iv-checkerは対象外**（別ブランディングの「GO BATTLE LEAGUE」eyebrowを据え置き）
- dpsだけにある`h1::before`のポケモンボールのアイコンは残す（raidには無い）
- **⚠ dpsは`template.html`と`dps/index.html`の両方を直す**（片方だけだと毎朝の自動更新で消える）
- **⚠ gbl/rocket/battlelogは`assets/gbl-app.js`の1つの`<header>`テンプレート（`#app`へ注入・既定はgbl用の`eyebrow`）を共用**。
  PAGE_ROCKET/PAGE_BLOGの分岐では h1（`innerHTML`で上書き）と同じく**eyebrowも`document.querySelector('header .eyebrow').outerHTML = ...`で置き換える**
  （`insertAdjacentHTML`だと2行に重複）。色はCSS変数`--ebc`のページ別クラス（`.eb-gbl`/`.eb-rocket`/`.eb-blog`・`assets/gbl.css`）

**⚠ ステータス図鑑(`/pokedex/`)のカード**: **`.card`全体に紫→水色の上端バー**（`.card::before`）。dps/raidの`.c-boss`等と同じ
「`position:relative`の`.card` + `::before`に`border-radius:15px 15px 0 0`」。**⚠ `overflow:hidden`は使わない**（検索候補`#sugg`が`.card`の外へ290pxまで伸びるため切れる）。
死んでいた旧`h1 b{background:linear-gradient(...);...}`は削除済み

**⚠ /gbl/ の名前は「GBLシミュレーター」（2026-09-10タダシさん指示）**: カード・見出し・`<title>`・manifest・他ツールのリンク（「GBLシミュレーター ↗」）・説明文すべて。
**このメモの過去の記述の「GBL対面シミュレーター」「GBL対面シミュ」は同じツールのこと**

**⚠ ホーム画面・デスクトップに置いたときの名前は、いまのツール名そのもの（2026-09-10タダシさん指示）**:
全13ツールの manifest の `name`・`short_name` と `<meta name="apple-mobile-web-app-title">`（dps は template.html にも）。**ツール名を変えたら、この3つも必ず一緒に直す**

**⚠ /gbl/・/battlelog/ のブランド表記は「GBL」**（「GOバトルリーグ」を短縮・2026-09-04）: 画面のH1・`<title>`・manifestの`name`が対象
（`battlelog`＝「GBL対戦記録」）。tooltip等の本文中の「GOバトルリーグ」は変えない。
場所は`gbl-app.js`のh1生成部＋`gbl/index.html`の`<title>`・`gbl/manifest.webmanifest`

**⚠「シミュレーター」（長音あり）で統一（恒久ルール・2026-09-04確定）**: 一般向けサイトなので内閣告示「外来語の表記」（1991年）に沿う（JIS Z 8301の長音省略は工業文書向け）。
リポジトリ全体を置換済み（`pvp-tests/`のfixtureは非公開で影響なし）。**新規の文言でも必ず「シミュレーター」**

**⚠ タイトル文字サイズは全ツール`1.45rem`（狭幅430px以下は`1.28rem`）（2026-09-04追加）**: 対象は dps・raid・type-dps・gym-attack・gym-defense・breakpoint・
iv-checker・pokedex・gbl/rocket/battlelog（`assets/gbl.css`）・max-battle。狭幅は`@media (max-width:430px){h1{font-size:1.28rem}}`

**ライトの色の出どころは「そのツールのアイコンの地の色」**（背景のにじみは全ツールほぼ同じで分かれないため）。
彩度は落とし気味。濃さは**背景とのコントラスト4.5:1以上でいちばん明るいところ**。**レイド火力・ジム挑戦・GBL対戦記録だけ彩度が一段上**（タダシさん選択）。

| ツール | ライトの色 | | ツール | ライトの色 |
|---|---|---|---|---|
| レイド火力 | `#ab4f3b` | | 対戦記録 | `#836625` |
| レイドシミュ | `#2b736f` | | ロケット団 | `#b14949` |
| タイプ別火力 | `#6863ac` | | ブレイクポイント | `#5868ac` |
| マックスバトル | `#ab4982` | | 個体値チェッカー | `#a44b76` |
| ジム挑戦 | `#91681d` | | ステータス図鑑 | `#7e60a4` |
| ジム防衛 | `#397a58` | | GBL対面 | `#4e67b1` |

ダークの`.wmk`色の例: dps=#ff7a5c／raid=#3fe0c4

**⚠ 取り寄せる文字（`text=`）は手で書かない**（抜けた字が端末の書体に化ける）。**ツール名を変えたら `python3 build_wordmark.py` を流す**（`assets/wordmark.css` は生成物）。
**トップページのサイト名は別物**（禅角ゴシック New 900・ブルーシルバー）だが取り寄せは wordmark.css が持つので、**トップページから wordmark.css を外さない**。

**⚠ `.wmk` を足すときは既存の見出しの色指定（`.tool` 等）を必ず消す**: `.tool{background:linear-gradient(...)...}` 等は`.wmk`と**詳細度が同点（0,1,0）**で、
後から読むページの`<style>`が勝つ（**ダークだけ**旧色になる。ライトは`:root.light .wmk`で隠れる）。`grep -n "header h1\|\.tool{\|h1 b{\|h1 span{"` などで洗い出す。

**⚠ ライトモードの文字色のコツ（2026-09-03タダシさん・恒久）**:

> **うっすらとした背景のときは、文字色も背景と同じ系統の濃い色にすると、雰囲気を壊さずに馴染む。**
> **ただし薄すぎると背景に溶け、濃すぎると浮く。ちょうどいい濃さを出すのがいちばん大事。**

**落とし穴**:

1. **大きい字（看板44px）と小さい字（18〜24px）でレシピを分ける**（金属加工を小さい字に流用すると折れ目が汚い線に・押し出しがにじむ・輪郭がぼける）
2. **暗い側を「黒」に寄せると色味が死ぬ**。暗い側は**色味を残した濃さ**
3. **`transform` をかけた部分には親の `background-clip:text` の塗りが届かない**（自分にも塗りを持たせる）
4. **輪郭は塗りの「下」**（`paint-order:stroke fill`）
5. **厚みは `text-shadow`**（`filter` だと h1 の中のバッジや添え書きにも影が乗る）
6. **押し出しは文字より暗い色**（明るい銀だと白くにじむ）

**トップページはタイル（2026-09-02・「トップページを本気で改善」）**（1列リストは最初の画面に2個半しか入らず不採用）:

| | 旧 | 新 |
|---|---|---|
| 看板の高さ | 275px | 195px |
| 最初のカードまで | 428px | 290px |
| ページ全体（375px幅） | 1883px＝2.3画面 | 1323px＝1.6画面 |
| 最初の画面に見えるツール | 2.5個 | **6個**（レイド・ジムが全部） |

- **タイルは縦組み**（アイコン60px→名前→短い説明）。**2列（スマホ）／3列（640px〜）／4列（860px〜）**
- グループ色は**上端の帯＋面のほのかな色＋アイコンの光彩**。**グループ見出しに件数バッジ**（6／4／2）
- **説明は短く言い切る**（「ボス別の火力ランキング」等）。**長い説明は📖説明モードへ**（`.ds.short`／`.ds.long`）
- **更新の頻度は看板に1行**（「データは毎日、対戦環境は毎週、自動で更新。」）

### サイト名は「GOナビ」（2026-09-02確定・タダシさん決定）

**サイト名は `GOナビ`、説明は `ポケモンGO 攻略ツール`**。表記は**半角英字＋カタカナの「GOナビ」で固定**（「ゴーナビ」「GoNAVI」と区別）。
基準は ①思い出せる ②32pxで残る ③商標で揉めない。

- **使えない候補（実在）**: ポケカチ／ポケバン／カチナビ／ポケラボ／ポケナビ／ポケGOナビ／トレナビ
- **商標**: 称呼「ゴーナビ」の登録が**3件**（城南進学研究社=41類／シーズ北海道=35・41・43類／プロトコーポレーション=9・12・35・41類）。
  商売は学習塾・地域情報・自動車情報で需要者が重ならず、危険度は低いと判断。**指定役務の全文は未確認**（気になったらJ-PlatPatで「ゴーナビ」を引く）

### ロゴ（2026-09-02確定・全アイコンの元）

**「GO」の O を方位磁針の輪に見立てる**。

| 項目 | 値 |
|---|---|
| 書体 | **Avenir Next Heavy**（`/System/Library/Fonts/Avenir Next.ttc` の index 8） |
| Gの高さ | 輪の直径と同じ（`glyph()` が二分探索で合わせる） |
| 輪の外径 | 画面幅の **0.245**（半径） |
| **輪の太さ** | **Gの縦線の70%**（= 画面幅の 0.112）。タダシさんが5段階から選択 |
| 針 | **マゼンタ**(255,90,190)／後ろ半分は青灰(140,152,198)／向きは **-38度**（右上＝前へ進む） |
| 中心 | 白い丸（半径0.019） |
| 目盛り | **なし**（案4の目盛りは不採用） |
| 地 | 濃紺のグラデーション＋3グループの色（水色・紫・赤橙）をほのかに散らす |

初代の作成スクリプト（`scratchpad/logo.py`→`logo3.py`→`logo_final.py`）は残っていない。
**Gと輪の太さは必ず比率で決める**（揃わないと文字と輪が別物に見える）。

**2026-09-08 「ナビ」を足した（確定）**: `scratchpad/logo_navi.py`（リポジトリに保存）。元の画素の「GO＋針＋光」を切り出して上へずらし
（背景は正規化畳み込みで周囲から復元）、下に **ヒラギノ角ゴ StdN W8**（`/System/Library/Fonts/ヒラギノ角ゴシック W8.ttc` の index 3）で「ナビ」。
色 **中=(166,210,255)・輪郭=(28,43,94)**。字の高さ117px・輪郭9px（512px基準）・**「ナビ」の上端はGOに少しかぶせる**（gap -8）・**GOは95%・全体を18px下へ**。
実行: `LOGO_SRC=<元の512> python3 scratchpad/logo_navi.py -8 117 9 3 0.95 18`（gap・字の高さ・輪郭・書体index・GOの倍率・下げる量）。
**⚠ 取り出し元は差し替え前のアイコン**（`git show a3e662e:assets/icons/home/icon-512.png` を `LOGO_SRC` で渡す。いまのファイルだと「ナビ」が二重になる）

### 全ツールのアイコン刷新（2026-09-01〜02・タダシさん指示）

**13個すべて「斜めのグラデーション＋ほのかな光＋四隅を少し落とす」・角の丸みもそろえる**。

| ツール | 変更 |
|---|---|
| ブレイクポイント | 棒グラフ（タイプ別火力とかぶり）→ **左上に「＋1」(Arial Black)＋右下に階段＋境目の金マーカー** |
| ロケット団 | 紫の分割背景 → **ほぼ真っ黒＋赤いR＋控えめな光沢** |
| 個体値チェッカー | 目 → **ゲーム内の評価バッジ**（ギザギザのふち・斜めの帯・ボール模様・黄→オレンジの星3つ）。地は白に近いピンク・帯が濃いピンク |
| マックスバトル | 雲＋盾剣 → **盾剣はそのまま、雲だけ赤黒い輪の雲**に差し替え。地は暗いマゼンタ |
| レイド | 怒った顔＋3人 → **伝説レイドのタマゴ風＋3人（だいだい色）**。地は深い青緑 |

**地の色**: レイド火力=深い赤茶／レイド=深い青緑／タイプ別火力=深い藍〜すみれ／マックスバトル=暗いマゼンタ／ジム挑戦=深い金茶／ジム防衛=深い緑／
対戦記録=深い金茶〜藍／ブレイクポイント=濃い紺（左上に金・右下に水色の光）／図鑑=深い紫／GBL対面=左右の分割のまま暗く。

**やり方の原則**:

- **絵は描き直さない。元の画素をそのまま使う**（描き直すと形が変わる）
- 地が**単色**なら `cut_out()`（地の色との差からalpha、ふちの色は地の混ざりを外す）。**ゆるいグラデーション**なら `cut_out_soft()`（四隅から地を推定・ゆらぎ `d0` は無視）
- **中身も暗い絵**（対戦記録）は、輪郭の角丸四角で切り出して**影を描き直す**
- **⚠ 取り出し元は「差し替え前のファイル」**（`git show HEAD:...`。上書き済みを読むと自分の絵を読む）
- 置き場所は中身の範囲(`getbbox`)から計算する（目分量にしない）
- **個体値チェッカーは自前のアイコンも持つ**（`iv-checker/favicon.ico` `icon-192.png` `icon-512.png` `apple-touch-icon.png`）。`assets/icons/iv-checker/` だけ直すとタブのアイコンが古いまま

**公式のマークは真似ない（恒久・2026-09-01）**: ゲーム内のマーク（サイドンの顔・マックスバトルの紋章）寄せは「パチモンアプリ感」で不採用。**題材（赤黒い雲・伝説のタマゴ）から自分たちのデザインを起こす**。

### テスターのフィードバックを集める仕組み（2026-09-06実装・タダシさん指示）

**テスターにDMでURLを送り、気づいたことをその場で送ってもらう**。タダシさんは特設ページで読み Claude Code に貼る。
**受け口は Tally**（無料・添付10MB・スプレッドシート連携・ログイン不要）。スプレッドシート＋スクリプト方式は警告画面の手間で不採用。

**送る側＝共通部品 `assets/feedback.css` / `assets/feedback.js`**（自己完結）。ページ側は2行（新ツールでも同じ）:
1. `<head>` に `feedback.css`（`home.css` の直後） 2. **`home.js` の直後**に `feedback.js`

- **設定は `feedback.js` 先頭の4つ**: `FORM`（空なら何も出さない）／`UNTIL`（過ぎたら消える）／`ALL`（`false`＝案内のURLを開いた人だけ・`true`＝全員）／隠しフィールド名 `tool`・`ctx`
- **⚠ `?fb=1` は `location.search` だけで判定しない**（GBL・ロケット団・対戦記録は先に住所を書き直す）。**`performance.getEntriesByType('navigation')[0].name` も見る**
- **⚠ 案内は全員には出さない**（既定 `ALL=false`）。URL末尾の `?fb=1` で端末に印（`site_feedback`）が残り全ツールでボタンが出る。**`?fb=1` は読み込み直後に住所から消す**
- トップページ（バナー）と全ツール（細い帯）に出す。（`ALL=false` なら `fb=1` で印の付いた端末だけ）。送る情報はツール名（`tool`）と、端末／ブラウザ・画面の大きさ・テーマ・日時・**そのときのURL**の1行（`ctx`）
- **見た目は金**（役割色と分けて一時的な案内と分かるように）。**絵文字なし**
- **⚠ 狭い画面では「テスト中」の札を出さない**（`.fbbar:not(.big) .fbtag`。左右余白の広いページだけ2段になる）。**⚠ 帯自身の左右余白10pxは外さない**（余白の無いページで端に貼り付く）。
  基準: 375px幅で全13ページ高さ47px・320pxでは2段

**読む側＝特設ページ `/feedback/`**（管理用・`noindex`・一覧に載せない）:

- **⚠ CSVのURLはファイルに書かない**（2026-09-13。GitHubで誰でも読めるため。旧 `CSV_URL` 直書きは廃止）。画面の「読み込み先のURL」欄から**端末にだけ保存**（localStorage `feedback_csv_url`）。
  過去のURLは履歴に残るので、**スプレッドシートを作り直して新しいURLにする**（タダシさんの作業）
- **フィードバック（`/contact/`）も同じフォーム**（ツール名の欄が「フィードバック」）。**個別の返事はしない**（連絡先の欄が無い。文言も「お返事はしていません」）
- 読み込みは **①公開CSVを自動で読む／②貼り付け欄**（読めなければ自動で開く）
- **⚠ 通し番号の列（Submission ID・Respondent ID）は「いちばん先に」外す**（あとだと日時の列探しで submission を掴む）
- **日時は `ctx` の端末の時計**（「Submitted at」は世界標準時）
- **列名は決め打ちしない**: `tool`・`ctx`・日時は見出しから、残りは中身から（URL＝スクショ／最長の文章＝本文／短い列＝名前）。**どれにも当てはまらない列は「その他」で必ず出す**
- **「この1件をコピー」「まとめてコピー」**で Claude Code に貼れる形（見出し・送信日時・環境・スクショのURL・本文）
- 確認した印（`feedback_done`）で既読を隠す（既定は未読だけ）
- **⚠ ライトの土台は `:not([aria-pressed="true"])` で限定する**（限定しないと**ライトだけ点灯しなくなる**）
- **⚠ 画像が読めなければ `onerror` でURLの文字に切り替える**（112pxの空箱が残るため）

**フォームを作るとき（タダシさんの作業）**:

1. `tally.so` で作る。質問は **気づいたこと**（長文・必須）／**スクリーンショット**（添付・任意）／**お名前**（短文・任意）＋**隠しフィールド2つ（必ず `tool` と `ctx`）**
2. 公開URLを `assets/feedback.js` の `FORM` に入れる
3. スプレッドシート連携をONにし「ファイル → 共有 → ウェブに公開 → カンマ区切り(.csv)」で公開、URLは `/feedback/` の「読み込み先のURL」欄へ（ファイルには書かない）。
   **⚠ 公開CSVはURLを知れば誰でも読めるので人に渡さない**
4. DMに貼るURLは **`https://gonavi.jp/?fb=1`**（旧 `tarcy009.github.io` も転送される）

### 「↻ 更新」ボタン（2026-09-08・ホーム画面から開いたときだけ・共通ファイル）

ホーム画面のアイコンから開くと（iPhoneの standalone 表示）更新手段が無いための対策。
`home.js` が **standalone のとき**（`navigator.standalone` または `display-mode: standalone/fullscreen/minimal-ui`）
**かつ開発者の端末だけ**（タダシさん判断: 使う人には意味が薄く、模擬戦の途中で押すとバトルが消える害のほうが大きい）、
`#themesw` の末尾に丸い「↻」（`.reloadsw`・見た目は🏠と同じ・`home.css`）を出す。ふつうのブラウザでは出さない。

- 開発者の印: **一度 `?dev=1` を開いた端末に `site_dev`**（`?dev=0` で解除・印は住所からすぐ消す）
- **⚠ iPhoneではホーム画面のアプリとSafariの保存領域が別**（Safariの `?dev=1` はアプリに届かない）。
  **アプリではタイトル(h1)を1.5秒長押し**で開発者モードON/OFF、画面下に「開発者モード ON」（`home.js` の `armLongPress`／`setDev`）。
  **素早く7回タップ**でも切り替え可（長押しが効かない端末の保険）。`?dev=1` はAndroid・PC用に残す
- **⚠ 長押しが「コピー」の吹き出しになって計測が切れる**対策: 計測は `touchstart/touchmove/touchend` で持つ（`pointercancel` では切らない）、
  `selectstart`／`contextmenu` を preventDefault、h1 は `user-select:none`＋`touch-callout:none`、CSSは**子要素まで** `user-select:none !important`
- **⚠ タイトルの7回タップで画面が拡大する**対策: `home.css` の `header h1, header h1 *` に `touch-action:manipulation`（2回タップの拡大だけ止める・ピンチは残る）。トップページの `header.hero` にも効く
- **⚠ トップページ(`index.html`)にも home.js／home.css を読み込む**（無いと長押しが「コピー」になるだけ）。🏠は出さないが、開発者モードと↻のため（explain の直後・feedback の前）。**↻はトップページにも出す**
- **⚠ `dev=1` は `location.search` だけで見ない**（feedback.js の `?fb=1` と同じ: GBL系が先に住所を書き直す）。
  `performance.getEntriesByType('navigation')[0].name`（最初に開いた住所）も見る
- 押すと Service Worker の更新を頼んでから `location.reload()`（2.5秒で終わらなければそのまま読み直す）。
  title に「反映まで1〜2分かかることがある（公開先の反映待ち）」
- **開発者の端末ではタブのアイコンに赤い丸**（2026-09-12タダシさん指示）: `home.js` の `devIcon()` が元のアイコン（`link[rel~="icon"]`）を canvas に描き、
  右下に白ふちの赤い丸を重ねて href を差し替える。開発者モードを切ると戻す。`setDev` と**別タブでの切り替え（`storage` イベント）**でもそろう。
  home.js を読むページ全部に効く（ページ側の追加不要）
- **⚠ 開発者モードは「?dev=1 で開いたタブ」だけ**（2026-09-13。端末の印だけで見ると全タブが開発者扱いになった）:
  **ブラウザではタブごとの印 `sessionStorage` の `site_dev_tab`**（タブ内の移動・再読み込みでは残る）、
  **ホーム画面のアプリだけ端末の印 `site_dev`**（開き直すとタブの印が消えるため）で判定。
  判定は **`home.js` の `window.GonaviDev()` 1か所**（snap.js の開発者用保存ボタン・タイプ別火力の透かしもこれを呼ぶ）。
  **開発者判定を新しく書くときも `GonaviDev()` を使い、localStorage を直接読まない**
- **タダシさんの方針: 開発者だけのための仕組みに時間とトークンを使いすぎない**。開発用の便利機能は最小限、使う人向けの作業を優先

### 公開に向けた案内ページ3枚（2026-09-10・タダシさん指示）

広告審査で見られる **運営者情報 `/about/`・プライバシーポリシー `/privacy/`・フィードバック `/contact/`**。
見た目は共通の `assets/infopage.css`（土台の濃紺・半透明のカード・1pxの罫線・絵文字なし・光彩なし）。

- **全ツールの最下部に3つのリンク**（`home.js` の `footLinks()` が body の最後に `nav#sitefoot` を差し込む・見た目は `home.css` の `.sitefoot`）。
  **トップページは自前のフッターに同じ3つ**（`footer .links`）。管理用の `/feedback/` には出さない。新ツールも home.js を読めば自動で付く
- **運営者名は `TARCY`**（タダシさん指定）。本名は書かない
- **フィードバックはテスター用と同じフォーム**（`assets/feedback.js` の `FORM` と同じURL・隠し項目 `tool=フィードバック`）。
  **⚠ 画面の呼び名は「フィードバック」（2026-09-20タダシさん指示）**。「お問い合わせ」だと運営者と個別にやり取りする印象になり、気軽な感想・不具合の報告が集まりにくいため。**URLは `/contact/` のまま**（アドセンス審査中はアドレスを変えない）。直す場所は contact/index.html（見出し・前書き・ボタン・隠し項目）・`home.js` の `footLinks`・トップページのフッター・運営者情報・プライバシーポリシー・`build_seo.py` の `PAGES`。
  ⚠ **フォームのURLを変えたら contact/index.html の `FORM` も直す**（2か所）
- **プライバシーポリシーは「広告（Google アドセンス）を利用することがある」「アクセス解析は使っていない」**の前提。
  **広告・解析を実際に入れたら該当の節を書き直す**（解析を入れたのに「使っていない」のままは不可）
- **他者の名前を書かないルールの例外**: **Google（広告・フォント）** と、著作権・商標の節の **任天堂・クリーチャーズ・ゲームフリーク・Niantic・株式会社ポケモン**だけ。
  ホスティングとフォームは「外部のサービス」と書く
- **テスター募集の帯は公開日の前日ごろに止める**（`feedback.js` の `UNTIL` を前日の日付に）

### 検索エンジン・SNS向けの準備 `build_seo.py`（2026-09-12実装・収益化の準備）

全ページの説明文・正式アドレス・共有用タグ・共有用画像と、`sitemap.xml`・`robots.txt`・`404.html`。

- **`python3 build_seo.py`** が作るもの（`--no-images` で画像だけ作り直さない）:
  ①各ページの `<head>` の **`<!-- seo:start -->〜<!-- seo:end -->`**（説明文・canonical・og:〜・twitter:card・JSON-LD）と `<title>`
  ②**共有用画像 `assets/ogp/<キー>.png`**（1200×630・濃紺の地にツールのアイコン＋英字の小見出し＋ツール名＋短い説明＋右下にGOナビ）
  ③**`sitemap.xml`**（18ページ・lastmod は git の最終更新日） ④**`robots.txt`**（/feedback/・/scratchpad/ 等を除外）
- **⚠ seo:start〜seo:end の中と `<title>` は手で編集しない**。`build_seo.py` の `PAGES` 表を直して流し直す。
  **ツール名・説明を変えたら必ず流し直す**（manifest の name・apple-mobile-web-app-title と同じく一緒に直すもの）
- **`<title>` は「ツール名｜ポケモンGOの◯◯｜GOナビ」の形**（タブで切れても読める）
- **⚠ レイド火力チェッカーは `template.html` と `dps/index.html` の両方を書き換える**（表の `src`）
- 画像のツール名は1行に入らなければ `NAME_BR` の `|` の位置で2行に折る（**語の途中で切らない**）。説明は「、」「。」「・」の後ろを優先して折り返す。
  **アイコンはツールのキーと同じフォルダを優先**（favicon からたどると個体値チェッカーが GOナビのアイコンになる。dps は `karyoku`）
- 案内ページ（運営者情報・プライバシー・フィードバック・データの引っ越し）の共有用画像はトップと同じ `home.png`
- **`404.html`**（手書き・noindex）: GitHub Pages が出す。13ツールのアイコン付き一覧＋トップへ。
  **どの深さのアドレスでも出るので、読み込むファイルとリンクは必ず `/` から書く**
- 旧 `iv-checker/ogp.png` はタダシさんの承認で削除済み
- **「使い方」の文章は最初からページ本体に書いておく**（アドセンス審査の手当て・タダシさん承認・**恒久**＝審査後も消さない）。
  画面をJSで組み立てるページは読み込み時点の本文が0文字で、検索エンジン・審査が中身を読めないため
  - **「使い方」は全16ページで最初から開いた状態**（2026-09-22タダシさん指示・審査に通すのを最優先）。ボタン `aria-expanded="true"`・箱に `display:none`/`hidden` を付けない（図鑑だけCSSが隠すので `style="display:block"`）。
    レイド火力・レイドシミュ・GBL系はボタンの文字も「（閉じる）」付き。**審査が通ったあとに閉じた状態へ戻してよい**（戻すときはこの3点を元に戻す）
  - レイド火力（`template.html`・`dps/index.html`）とレイドシミュ：文章は `#helpbody` の中に直接書く（JSの `HELP_HTML` は廃止）
  - **GBL・ロケット団・対戦記録：元の文章は `assets/gbl-app.js` の `HELP_HTML` の1か所**。`python3 build_help.py` が各ページの
    `<!-- help:start -->〜<!-- help:end -->`（`#helpsrc`）へ写し、gbl-app.js が読み込み時に `#helpbody` へ移す。
    **HELP_HTML を直したら必ず `python3 build_help.py` を流す**（ロケット団は `PAGE_ROCKET` の節を省いて写す）
  - 読み込み時点の本文：GBL・対戦記録 0→約12,800字／ロケット団 0→約3,800字／レイド火力 312→約3,300字／レイドシミュ 486→約3,300字

### 公開までの段取りと公開後の検索対策（2026-09-12〜13・タダシさん決定）

**段取り**: テスター募集（DMのURLは **`https://gonavi.jp/?fb=1`**・「Safariなどのブラウザで開く（ホーム画面のアプリでは送るボタンが出ない）」
「開けないときは再読み込み」と添える・帯の期限は10/31）→ **公開日はアドセンス審査が通ってから決める** → 公開と同時に動画で宣伝。
公開日の前日に feedback.js の UNTIL を止める。

- **審査中の修正**: 不具合・文言・見た目の修正や機能追加は可。**ページを消す・アドレスを変える・サイトが開けない時間を作る・
  「準備中」のページを置く・ドメインや表示の仕組みを変える**のは審査が終わるまで避ける
- **旧アドレスのホーム画面アイコンは gonavi.jp で追加し直す**（旧アドレスの古い仕組みは直せない）。開発者モードは新しいアイコンでタイトル1.5秒長押し
- **タダシさんは「スマホ検索の順位を下げること」を絶対に避けたい**。はじめて来た人に本文を覆う小窓を自動で出さない（かんたん案内の項）

**検索作戦**（まとめのページ「GOナビ 検索作戦」: https://claude.ai/code/artifact/f42fb314-2d39-4605-889a-6350aeeb21c7 ）:
1. 公開初日から、動画の説明欄・固定コメント・X に**中身に合うツールのURLを直接**貼る（タダシさん・いちばん早く効く。旧アドレスは貼らない）
2. 公開後は週1回、Search Console の「検索パフォーマンス」で、表示が多いのにクリックが少ない言葉にタイトル・説明文を寄せる
3. **審査が通ったら「1ポケモン1ページ」を作る**（最大の伸びしろ・まず図鑑から。`/pokedex/<名前>/` 約1,250ページを毎朝の自動更新で）。次にレイドボス別
4. イベント・シーズンの前にページと動画を出しておく
- 見通し: 新しいドメインが上位に来るまで3〜6か月。短い言葉は大手が強く、狙うのは長い言葉と名指し（「GOナビ」）

**個別ページの中身（2026-09-13決定）**＝そのポケモンの「評価・使い道」のページ（「〇〇 評価」「〇〇 おすすめ技」に答える）。
各ツールの結果からそのポケモンの分を抜き出す — 図鑑（種族値・最大CP・わざ＝全ポケモン）／タイプ別火力（そのタイプで何位）／
レイド火力（いまのボスで10位以内のものだけ）／マックスバトル（アタッカー・タンクの順位）／ジム防衛・耐久指数／
GBL（3リーグの環境勝率と順位・確定わざ・圧勝できる相手／苦手な相手を数匹）／個体値チェッカー（リーグ別1位の個体値とPL）。
- **活躍しない項目は出さない**（載る項目がポケモンごとに違う＝薄いページにしない）
- **ツールと同じ計算で出す**（食い違い禁止）。「◯月◯日時点」を載せる。GBLの計算は pvp-engine.js なので、GitHub Actions では node で回す
- **各項目からそのポケモンを入れた状態のツールへリンク**
- 良し悪しの言葉は使わず事実を並べる（「環境勝率58%・スーパー7位」）
- **まず10匹ほどの見本をタダシさんに見てもらい、形を決めてから全ポケモンへ**。検索される見込みの高いものから段階的に公開し、Search Console で登録のされ方を見ながら増やす
- ⚠ Google はサイト全体でも「役に立つ中身か」を見る。**中身の薄いページの割合**が多いと良いページまで順位が下がり、
  手動による対策（検索から外される）・アドセンスの広告制限もありうる。問題は数ではなく中身の薄さ

### 1ポケモン1ページ用の出口（2026-09-21新設・各ツール本体の「全順位をデータで返す口」）

1ポケモン1ページは**ツールと同じ計算で出す**（食い違い禁止）ため、数字は画面なしブラウザで**各ツール本体を開いて出口を呼んで**集める。**出口は画面を一切変えない**（状態を控えて戻す）。

| ツール | 出口 | 返すもの |
|---|---|---|
| タイプ別火力 | `GonaviExport()` | 17タイプ×（base＝絞り込みなし／all＝シャドウ＋メガ・ゲンシ）の全順位（分割SP優遇OFF） |
| 耐久指数 | `GonaviExport()` | base＝既定／all＝シャドウ＋メガ・ゲンシ＋進化前 |
| ジム防衛 | `GonaviExport()` | 5つの並べ替えの全順位（`rank`＝二重弱点を除いた順位・`rankAll`） |
| マックスバトル タイプ別 | `GonaviExport()` | 18タイプのアタッカー・タンク・ダイウォール込みタンク |
| ジム挑戦 | `GonaviGym` | 計算の部品（`defCtx`／`hitOf`／`comboOf`・`opts()`）。**render も同じ関数を呼ぶ**。`hitOf` の5番目の引数 k は耐え数のカーブ r/(r+k)（**ツールは2で固定**・ページの評価だけ3） |
| GBL・ロケット団 | `GonaviGbl.env(key, shadow, cap)`／`.rocket(kind, あいてkey)` | 環境一覧にポケモンを入れただけの状態（既定のわざ・理想個体）の結果／`rkRankFor` の順位 |

- **⚠ 出口は画面と同じ関数を呼ぶ形にする**（出口のために計算を書き写さない）。GBLは `multiCellsOf`・`listSideCfgOf`（`S[i]` の代わりに状態を渡せる形）を環境一覧と共用
- **⚠ ランキングの計算・表示を変えたら出口も見る**。答え合わせ `exit-rank`／`exit-gbl` が「出口＝画面」を保存の前に確かめる（わざと崩すと不合格になることを確認済み）
- 集める係・ページを組むスクリプトは `pokepage/`（下の節）。見本づくりの試作は `scratchpad/pokepage/`（git管理外）

### 1ポケモン1ページ `pokepage/`（2026-09-21・公開はアドセンス合格後・タダシさん決定）

**`python3 pokepage/run.py --publish`** が 集める（`collect/`＝各ツールの出口）→ 育成情報（`gm_all.py`）→ コスチューム → ページの一覧（`roster.py`・1,120ページ）→ 段階（`tiers.py`）→ 組む（`build.py`）→ 点検（`check.py`）を約40秒で流す。
`--publish` なしは確認用（`pokepage/out/`・全部 noindex・一覧は `pokepage/out.html`）。途中のデータ `pokepage/data/` と確認用は git に入れない。

- **出力**: `/pokedex/<キー>/index.html`・`assets/pokepage.css`（全ページ共通の見た目）・`pokedex/pages.json`（名前→キー。ステータス図鑑の入口が読む）・`sitemap-pokemon.xml`
- **公開スイッチ**: 毎朝の自動更新（`update-data.yml` の最後の4つ）は**リポジトリの変数 `POKEPAGE` が `on` のときだけ**動く。`test` は作って点検するまで（公開しない）。
  **⚠ この端末では公開まで `.git/info/exclude` で上の4つの出力を git から隠している**（合図のときにその5行を消す）
- **⚠ 日付だけが変わったページは書き換えない**（`write_if_changed`。毎朝1,120ページが書き換わって履歴が膨らむのを防ぐ）。サイトマップの lastmod も中身が変わった日
- **⚠ 数字はツールの出口の値をそのまま使う。ページ側で数え直さない**（ジム防衛の二重弱点のポケモンを「二重弱点を除いた中」で数え直し、ツールのどこにも無い順位を出していた。二重弱点は「二重弱点も」を押したときの順位＝`rankAll`）
- **点検（`check.py`）**: リンク切れ・None等の混入・英語のID・タイトル/説明文の重複・表記の決まり・**タイルの数字と出口のデータの突き合わせ**（レイド・ジム防衛・マックスバトル・GBL・ロケット団）。1つでも問題があれば終了コード1＝公開しない。
  自動更新ではその前に答え合わせ `exit-rank`・`exit-gbl`（出口＝画面）も流す
- **⚠ アドセンスの審査が終わるまで全ページ noindex・サイトマップは空**（`build.py` の `HOLD = True`・2026-09-22タダシさん指示。2026年8月18〜21日のGoogleのスパムアップデートが大量の自動生成ページを狙ったため）。ページは消さない。**合格したら `HOLD = False` にして段階的に載せる**。点検は `python3 pokepage/check.py --publish`（引数なしは確認用の古いコピー pokepage/out/ を見る）
- **検索エンジン**: サイトマップに載せるのは `build.py` の `STAGE`（いま1）までの段階。載せない候補（強い場面0・進化先に①なし・固有の仕様なし）は noindex。canonical・og・パンくず（JSON-LD）はページごと。
  robots.txt の `Sitemap:` の行は `sitemap-pokemon.xml` が git に入ってから `build_seo.py` が足す。`/pokepage/` は robots.txt で除外
- 共有用画像は全ページでステータス図鑑のもの（`assets/ogp/pokedex.png`）を共用。**ポケモンごとの画像は作らない**（2026-09-21タダシさん決定・1,120枚は重く、一部だけ作るのも中途半端なため）
- ステータス図鑑のヒーローカードの下に「◯◯の強さと使い道 ›」（`#pglink`）。`pages.json` が無ければ出ない＝公開前は何も出ない。コスチューム違いは括弧の前の名前のページへ
- 一覧から外れたページは消さずに知らせるだけ（消すのはタダシさんの確認のあと）
- **公開の日の手順**: ①`.git/info/exclude` の5行を消す ②変数 `POKEPAGE` を `on`（または一度 `test` で試してから）③自動更新を手動で1回流す ④公開されたら `python3 build_seo.py --no-images` で robots.txt にサイトマップの行を足してpush ⑤Search Console に `sitemap-pokemon.xml` を送る ⑥段階を広げるときは `STAGE` を上げる

### サイトの画像にはGOナビの透かし・開発者の端末だけ透かしなし（2026-09-12・タダシさん指示）

共有されたときにサイトの画像だと分かるよう、サイトが作る画像にGOナビのロゴの透かしを入れる。
**動画編集で使うタダシさん（開発者）の端末だけは透かしなし**。

- 対象はいまのところ**タイプ別火力ランキングのグラフ**。**画面のグラフにも保存する画像にも入る**（画面のスクショで共有されるため）
- 中身: **右上の角にロゴ**（不透明度.92）＋**グラフの真ん中にごく淡い大きなロゴ**（.07・角を切り取られても残る）。
  `type-dps/index.html` の `watermarkSVG()`（右上の表記は「画像を保存」の節の「ロゴ＋gonavi.jp」の決まりに従う）
- **⚠ SVGを画像にして保存するとき、SVGの中から外部の画像は読まれない** → ロゴは読み込み時に `fetch` → data: の形（`LOGO_URI`）で埋め込む。
  読み込み前はロゴ無しの文字だけで描き、読めたら描き直す
- **開発者の端末＝`https://gonavi.jp/type-dps/?dev=1` を開いた端末**（`home.js` の `site_dev`。どのツールのアドレスでもよい。解除・アプリでの長押し・判定は「↻ 更新」の節）。
  透かしが消え、保存ボタンが「📷 画像を保存（ロゴなし・開発者）」になる
- **⚠ 完全な秘密ではない**（ソースから読める）。「透かしのない画像が作れる」だけで害は小さい、という判断
- **新しく「画像を保存」の機能を作るときも透かしを入れ、`devClean()` のときだけ外す**

### 画像を保存 `assets/snap.js`（2026-09-12実装・タダシさん指示）

**全ページ（GBL対戦記録・フィードバック一覧を除く）が `/assets/snap.js` を読む**（`</body>` の直前・1行）。新しいページにも同じ1行を足す。
**⚠ 対戦記録には付けない**（タダシさん指示）。snap.js 自身も `/battlelog/`・`/feedback/` では何もしない。
ページが自前で画像を組み立てる用に `data-snap-nodev` と `GonaviSnap.preview/busy/stamp/isDev` がある。

- **① 一般向け＝ランキングの「📷 画像を保存」**（snap.js の `PUB` 表に設定があるページだけ）:
  レイド火力(`#rankList`)・耐久指数・ジム挑戦(`#ranklist`)・ジム防衛(`#list`)・マックスバトル(`#list`)・個体値チェッカー(TOP10の表)。
  **幅720で作る**（スマホでも同じ形）。枠（濃紺・角丸・上端にツールの色の帯）に
  **英字の小見出し＋ツール名＋条件（ボス名など `ctx`）＋点灯中の絞り込み（`tags`・ピル）**、**右上にGOナビのロゴ・gonavi.jp**
  （`drawBrand`。右上＝トリミングで切られにくい。1920×1440も同じ。**日付は出さない**・ロゴと文字をまとめて1.35倍・右端そろえ）。
  **開発者の端末ではロゴ・アドレスを出さない**（ボタンも「ロゴなし・開発者」表記）。
  ランキングを増やすときは `PUB` に1行足す（target=画像にする要素・rows=1行の要素・drop=外す要素・barIn=ボタンの置き場所）
- **② 開発者向け＝左下の📷（撮影モード）**: `site_dev` の端末だけ。**画面のどこでもタップした部分**を選べる（マウスは乗せるだけで候補が光る）。
  上のバーで **▲外側／▼内側（ひと回り広げる・戻す）／背景あり・透明（既定は透明）／保存／✕**。
  **開発者の端末で保存する画像はすべて色調整済み**（2026-09-17タダシさん指示・動画編集の手間を省く）: `preview` の最後とタイプ別火力の `saveGraph` で `devGrade`（`DEV_GRADE` の色の行列＝彩度・明るさ・黒の締まり。タダシさんの調整前後の2枚から割り出した）。一般の人の画像には掛けない
  **⚠ 枠の外に背景を付けて二重の枠にしない**（2026-09-17タダシさん指示）: 枠ごとの保存ボタンは常に枠の外を透明、撮影モードも既定は透明キーボードは ↑↓・Enter・Esc。
  **画面の見た目どおり**（その時の幅・テーマ・📖説明モードのまま）・2倍の解像度・ロゴなし
- **②' 開発者向け＝枠ごとの保存ボタン**: 開発者の端末では**画面の主な枠の右上に「📷 画像を保存」を直接出す**（**「1920×1440」は `data-snap-bigw` の枠だけ**＝中身を横長に組み直せる所。ほかは透明な余白が増えるだけなので2026-09-17に外した）（`renderDev`・1秒ごとに置き直す）。
  枠の判定はクラス名でなく **「角丸8px以上・枠線/影/背景のどれかがある・幅が画面の半分(最大280px)以上・高さ70px以上の、いちばん外側の箱」**
  （`findFrames`。header・nav・固定表示は除く。枠の中の箱には付けない）。
  ボタンは `#snapdevlayer`（文書の座標で絶対配置）から**枠の上辺にまたがる位置**に浮かせる＝中身に重ねない・overflow:hidden でも切れない。
  模擬戦の全画面（`.bfull`）・見本の表示中・撮影モード中は隠す。保存の中身は撮影モードと共通（`devSave`）
  - **画像のときだけの見た目は `data-snap-cls="クラス"`**（複製にだけクラスを付け、CSSで見た目を持つ）。**`data-snap-bigw="幅"`** があると1920×1440はその幅で描き、
    複製にさらに `snapbig` を付けて余白24pxで置く（2026-09-16）。第1号は **GBLの1対1の結果（`#result`・`.snapd`／`.snapbig`・gbl.css末尾）**:
    ブレイクポイントのボタンは写さない（`data-snap-skip`）・9マスを大きく・勝ち＝緑／負け＝赤の面・両者の名前の前に「じぶん／あいて」の札・
    9マスの上に金の帯「あいて｜◯◯のシールド →」と左に水色の縦帯「じぶん｜◯◯のシールド ↓」（`.mtxt`/`.mtxl`/`.sidetag` は画面では隠す）。
    1920×1440は横長の配置（上＝両者・下の左＝9マス・下の右＝わざ）で、枠の縦横比を画像に合わせ9マスで高さを埋める（幅1180で描く）
    **画面にもこの見た目を使う**（タダシさん指示）: `#result` は常に `class="result snapd"`（画像のときは複製に `snapshot` を足して「タップで切替」を隠す）。
    枠はテーマによらず濃紺、中の色の変数もダークの値に置きかえ（ライトでも読める）。540px以下で字と余白を詰める
  - **画像に写したくない部分には `data-snap-skip`**（複製時に取り除く）。レイドシミュの結果の枠はタイムライン（`details.tl`）に付けてあり、設定・人数の表だけが写る（2026-09-16）
  - **⚠ ランキングの一覧（`PUB` の target）は一覧全体を1つの枠にする**（包む枠が無いと1行ずつにボタンが並ぶ。ジム防衛・ジム挑戦・マックスバトル）
  - **⚠ 押した瞬間に枠がまだ画面にあるかを確かめる**（図鑑で古い枠を撮っていた）。画面が変わったら0.15秒後にも置き直す（MutationObserver）
  - **固定表示（`position:fixed`）は枠にしないが、画面をほぼ覆う固定の幕（図鑑のわざ詳細ウィンドウ `.mvov` 等）は中を見に行く**（2026-09-16）。
    幕が開いているあいだは**幕の中の枠だけ**にボタンを付ける（後ろの枠のボタンが透けて出るため）。幕の中のスクロールは `scroll`（capture）で `placeDev` だけ呼び直す。
    親の複製は `position:static`・背景なしに中立化されるので、暗い幕は写らない
  - 基準（開発者モードの枠ボタン数）: レイド火力3・レイドシミュ3・タイプ別火力3・マックスバトル2・ジム挑戦2・ジム防衛1・GBL2・ロケット団3・
    ブレイクポイント2・個体値チェッカー4・図鑑3・耐久指数1・対戦記録0（出さない）・トップページ0
  - **⚠ ローカル確認: 開発者モードのページは `--dump-dom` が終わらない**（1秒ごとの見直しで仮想時間が進み続ける）。数えるときは確認用ブラウザで
    `dispatchEvent(new Event('resize'))` してから `#snapdevlayer` の子の数を見る
- **④ 一覧の画像は上位5件・10位までは「📊 グラフ画像」**（タダシさん指示）:
  - **一覧の画像（ふつう・1920×1440とも）は上位5件**（`LIMIT=5`）。1920×1440は**1列のまま、枠の形に合う幅で描き直して**収める
    （幅720で一度描いて高さを測り、`高さ×枠の横長比` の幅(560〜1100)で描き直す）。「2列」の仕組みは廃止。
    **個体値チェッカーだけ10位まで**（`PUB` の `limit:10`・2026-09-17タダシさん指示）。見出しは**ポケモン名＋「◯◯リーグ　／　個体値順 TOP10」**（`ivHead`）。
    **ツール名「個体値チェッカー」と、入力した個体の順位・CPは画像に出さない**（動画で大事なのはポケモン名・リーグ・個体値順の3つ）。「あなたの個体」の点灯も外す（`strip`）
  - **個体値チェッカーのボタンは表の上の1列だけ**（2026-09-17・渋滞の解消）: 「📷 画像を保存」＋開発者だけ「表だけ」「進化系統」（`devBtns`）。
    1920×1440は出さない（`no1920`）・枠ごとのボタンも出さない（`devFrames:false`）。
    **「進化系統」＝系統の姿ごと×スーパー・ハイパー・マスターの上位5位を1枚ずつ**作り（**マスターは最終進化の姿とメガ・ゲンシだけ**・**最大CPがCP制限に300以上届かないリーグは作らない**・ページの `GonaviFamilyImages`）、一覧（`previewMany`）で1枚ずつ／すべて保存。
    攻撃優先のときもスーパー・ハイパーは個体値順で作る
  - **見出しの条件の行は、右上のロゴの手前（`BRAND_W`）で、「／」の項目ごとに折り返す**（`wrapCtx`・2026-09-17。個体値チェッカーでSCPがロゴに重なった）
  - **`data-snap-frame`＝外側の枠の中にある要素にも開発者の保存ボタンを出す**（個体値チェッカーのTOP10の表）。地が半透明なので、画面で透けて見えている色を下に敷いて描く（`under`＝`rowFill`）
  - **「📊 グラフ画像」**（`publicGraph`）: レイド火力・マックスバトル・ジム挑戦・ジム防衛・耐久指数（`PUB` に `graph` があるページ）。
    **上位10件の縦棒グラフ・1920×1440**。タイプ別火力のグラフと同じ作り（細めの棒・光彩・棒の中に順位・上に値・下に名前・シャドウは炎マーク・
    右上にGOナビ・真ん中に淡いロゴ・角丸と青系の枠）で、**棒の色だけツールの色**（`--wmk-d`）。見出しは「前書き（英字の小見出し）→（ボス名など `head`）＋ツール名＋TOP10」、
    点灯中の絞り込みは右上のピル
  - **名前と値は画面の一覧から読む**（`graph` の `name`/`val`/`shadow` セレクタ・値は最初の文字の塊）＝**画面の順位・数値と必ず同じ**。
    順位は行の「◯位」から読む（同順位もそのまま）。名前は最大3行、**括弧のある名前は括弧の前で折る**
  - 棒の目盛りの下限は30に固定（2026-09-16タダシさん指示・タイプ別火力と同じ。刻みは `niceStep`＝1・2・5刻み）。
    30を切る値があるとき・耐久指数のように30が刻みに乗らないときだけ0から。耐久指数は3桁区切り
  - 個体値チェッカーの表とタイプ別火力には付けない（個体値の表はポケモンの順位ではない・タイプ別火力は元からグラフがある）
- **右上のGOナビは「ロゴ＋その下に gonavi.jp」だけ**（2026-09-16タダシさん指示。「GOナビ」の文字は出さない・ロゴを少し大きく・右端そろえ）。
  一覧の画像（`drawBrand`）・📊グラフ画像・タイプ別火力のグラフ（`watermarkSVG`）の3か所。グラフでは右上の札をロゴの左隣に並べる（ロゴの幅128pxぶん左へ）。
  **グラフの真ん中のうっすらロゴはタダシさんのお気に入り**。位置は中央から高さの17%上
- **マックスバトルの1920×1440は左右4位ずつ・8位まで**（`PUB` の `l1920: {type:'grid', per:4, inlineTags:true}`・タダシさん選択）。
  列の幅は4行ぶんの高さから決めて枠いっぱいに収める。絞り込みの札はボス名の右横（`drawHead` の inline）。
  `publicImage1920` の `'two'`/`'compact'`/`'narrow'`/`'top3'` は比較用の試作（未使用）
- **保存する画像は全部「四隅が丸く・枠の外は透明のPNG」**（タダシさん指示）:
  共通の `roundFrame(canvas, edge)`（丸みは幅1920で半径34の比率・edge=true で青系の細い縁取り）。
  1920×1440のランキング画像は縁取りつき、ほかは `preview` が最後に角丸だけ当てる（開発者の保存・撮影モードも含む）。
  もとから角丸で描く画像（一般向けの幅720・グラフ画像）は `__rounded` の印で二重に切らない
  - **⚠ 「背景あり」で画像の下端に青い横じまが出る**（画像の高さは枠の影込みなのに、背景の箱 `.snapbody` が中身の高さまでしか無いため）→
    描き直すときに箱の `min-height` を画像の高さにする（`svgOf` の withBg）
- **画像のピルの表記**（タダシさん指示）: **「シャドウ含む」「メガ・ゲンシ含む」「メガLv4」**。
  `tagLabels` で言い換える（シャドウのボタンは文字が無く aria-label「シャドウを含める」を拾うため・メガLvは数字だけのボタンのため）。
  グラフ画像の炎マークは「シャドウで始まる」ピルに付く
- **③ どこでも2つ目のボタン「1920×1440サイズ」**（タダシさん指示「動画でよく使うのはこっち」）:
  - **ランキング**: 1920×1440ちょうど・角丸なしの全面。見出し・ロゴは1.8倍。中身は縦の中央。`publicImage1920`
    （件数・並べ方は④とマックスバトルの項に従う）
  - **タイプ別火力のグラフ**: `graphSVG(forSave, W)` の幅を1920にして描く（右側の位置は `W` から＝`R=W-52`・ピル `W-70`）。ボタンは `#saveBtn2`
  - **撮影モード**: バーの「1920×1440」＝選んだ部分を中央に大きく置く（余白60）。背景ありならページの背景（`pageBg`）を全面に敷き、
    選んだ部分は透明で描いて重ねる（背景ありで描いて重ねるとグラデーションの継ぎ目が出る）。描く倍率は2〜4に上げる
  - ファイル名の末尾に `_1920x1440`
- **⑤ 開発者向け＝「狭い画面 1〜3位／〜5位／〜8位／〜10位」**（2026-09-13タダシさん指示・`NARROW`／`narrowImage`／`setupNarrow`）:
  レイド火力・タイプ別火力・マックスバトル・ジム挑戦・ジム防衛の一覧の直前に、**開発者の端末だけ**紫の4ボタン。
  **パソコンのブラウザを最小まで縮めた幅（500px）の見た目**で、上位N位の行だけを、**行の外（行間も）は透明**のPNGにする。
  **マックスバトルだけ640px**（行の文字が多い・タダシさん指示）。倍率3・いまのテーマのまま・ロゴなし・見出しなし
  - しくみ: 一覧を幅 `w` で組み、左右に余白を足して全体を `vw`（500/640）にする＝@media がその画面幅で判定される。
    描いたあと透明な周りを切り落とし（枠の影ぶん4px残す）、仕上げの角丸はかけない（枠の角を削るため）
  - **`w` は実測値**（画面幅500で dps 448／type-dps 446／ジム挑戦・ジム防衛 468、640でマックスバトル 608）。
    **一覧の余白・幅の指定を変えたら測り直して `NARROW` を直す**
  - **枠の中は背景あり・枠の外は透明**（タダシさん訂正）。レイド火力・タイプ別火力の行は自分の背景を持たないので、
    **自分の背景が無い行にだけパネルの背景を敷く**（`rowFill`・`prepClone` の `fill`。パネルが半透明ならページの地の色を下に足す）
  - **4位以下も画像のときだけ独立した枠に入れる**（タダシさん指示）: 上の線が無い行にだけ `NARROW_FRAME`
    （線＝`var(--line)`・角丸12px・間隔3px・左余白12px＝1〜3位の枠と同じ形）。画面の見た目は変えない
- **保存のしかた**: まず**見本（プレビュー）で見せ**、「保存する」で保存（スマホは共有メニュー＝写真に保存・パソコンはダウンロード）。見本の長押しでも保存可。
  **⚠ 押した瞬間に保存しない**（画像作りに時間がかかると、iPhoneが「ユーザーの操作ではない」として保存を断る）
- **しくみ**: 要素を複製 → ページのCSSを書き写す → SVGの foreignObject に入れて canvas に描く（外部の部品なし）
  - **⚠ SVGを画像にするとき外部のファイルは読まれない** → 画像・書体（Google Fonts の @import も）を data: で埋め込む。
    文字を絞っていない日本語書体（url が24個を超えるCSS）は取らない＝その書体だけ端末の書体で描かれる
  - **⚠ 親のクラスで効くCSSがある** → 親は「箱の飾りを消した浅い複製」（`.snapanc`・::before/::after も消す）で包む
  - **⚠ `:root` / `html` / `body` を狙ったCSS** → `.snaproot` / `.snapbody` に書き換える
  - **⚠ @media**: 一般向けは幅720のまま SVG の中で判定（＝パソコンの見た目）。撮影モードは**いまのブラウザの幅**で判定して中身だけ残す
  - 一般向けは**常にダーク**（ブランドの見た目をそろえる）。撮影モードはいまのテーマのまま
  - 大きすぎる要素は倍率を自動で下げる（面積2億画素・一辺15000pxまで）
  - **⚠ Chromeの不具合**: `background:var(--x)` のような**変数入りの省略形のあとに `background-clip:text` などの個別指定**があると、
    `style.cssText` が `background-image: ;` と空で出る（`getPropertyValue('background')` も空）。
    → **CSSファイルの元の文章から同じセレクタの塊を探し、変数入りの省略形だけ取り戻す**（`ser`・`findBlock`・`rawOf`）。
    直さないとマックスバトルのポイントの数字（グラデーションの文字）が画像から消える
  - **⚠ 先に測った高さとSVG内で描いた高さは少し違う**（書体の差で行が高くなり、最後の行が切れる）
    → **高さを多めに取って描き、下の透明部分を切り落とす**。背景ありは背景なしで一度描いて高さを決め、背景ありで描き直す
  - **⚠ ローカル確認**: 非表示のブラウザパネルでは setTimeout が極端に遅い → 確認スクリプトは**押すだけ・始めさせるだけ**にし、結果は別の呼び出しで受け取る。
    **ブラウザが古い snap.js を覚えている**ことがある（転送0バイト）→ `?v=時刻` を付けた script を足して試す
- **確認のしかた（ローカル）**: `window.GonaviSnap.renderEl(el, {...})` かボタン→見本の `<img>` を `fetch` し、scratchpad の受け取り口（POSTをファイルに書く小さなサーバー）へ送って見る（パネルではダウンロード不可）
- **⚠ iPhoneのSafariで foreignObject の画像化が断られる可能性は実機で未確認**。断られたら「画像を作れませんでした」と出る
- `www.` と旧アドレスは gonavi.jp へ301で転送。パネルの「An unknown error occurred when fetching the script.」は SW 登録がパネルで拒まれるだけ（ツールのエラーではない）
- **証明書**: Settings→Pages が「DNSチェック進行中」のまま止まったら**「取り除く」を押す**（チェックがやり直されて通り、ドメインは消えない）
- **Search Console は「URLプレフィックス（https://gonavi.jp/）＋HTMLファイル」で登録**。
  **`google6fae09f6e5c5311a.html`（リポジトリ直下）は消さない**——消すと所有権の確認が外れる
- 「HTTPSの強制」ON・Search Console に sitemap.xml 送信済み（18ページ検出）
- **アドセンスはYouTube収益化で使っていた既存アカウントを「再有効化」して申請**（新しいアカウントは作らない＝1人1アカウント）。
  所有権の確認は「ads.txt スニペット」方式、**リポジトリ直下の `ads.txt`（発行元ID `pub-7623378724303874`）は消さない**
- **残り**: アドセンスの「審査をリクエスト」→ 審査が通ったら広告のタグを全ページへ

### 開発者だけの「🚫 除外」`assets/devex.js`（2026-09-19実装・タダシさん指示）

このサイトは**近々登場するポケモンを先行して入れている**（メガムクホーク・`MANUAL_MEGA`／`MANUAL_RELEASED`／`MANUAL_SHADOW` など）。
動画を作るときに未登場のポケモンが一覧に混ざると困るので、**開発者だけがピンポイントで指定して外せる**ようにした。

- **入れたのは8ページ**: レイド火力（`template.html`＋`dps/index.html`）・タイプ別火力・ジム挑戦・ジム防衛・
  マックスバトル対策・**マックスバトル タイプ別アタッカー／タンク**（中身は共通の `max-type/max-type.js` に1回だけ書く）・耐久指数
- **開発者の端末だけ**（`GonaviDev()`）。ふつうの人にはボタンも出ないし、`has()` が常に false なので**一覧は1匹も減らない**
  （保存が残っている端末でも、開発者モードを切れば元どおり＝実測で確認済み）
- **リンクで渡せる**: `?devex=メガムクホーク,ウッウ`（開いた端末に覚えさせて住所からすぐ消す。`?dev=1` と同じ流儀で
  `location.search` だけを見ない）。**`?devex=` だけ（空）で全部外す**。パネルの「🔗 このリンクをコピー」で作れる
- **順位は外したあとで振り直す**（データを作る段階で外すので、件数・グラフ・保存する画像まで自動で正しくなる）
- **「シャドウ○○」は「○○」と同じ扱い**（1つ指定すれば通常もシャドウも外れる）
- 保存キーは **`site_devex`**。**⚠ 開発者だけの設定なので「データの引っ越し」（`/backup/` の `SKIP`）から外してある**（`site_dev` と同じ）

**ページ側にやること（新しいランキングを作るときも同じ）**:

1. `<head>` に `<script src="/assets/devex.js"></script>`。**⚠ ページ本体のスクリプトより先に読む**
   （後ろだと `window.GonaviDevEx` が未定義のままボタンが出ない。実際に踏んだ）
2. 一覧を作るところで `if (window.GonaviDevEx && GonaviDevEx.has(名前)) continue;`
3. 作り直しの合図 `GonaviDevEx.on(() => 描き直す関数())`
4. **⚠ わざ構成などをためこむキャッシュの鍵に `GonaviDevEx.sig()` を混ぜる**（メガLv・分割SP優遇と同じ落とし穴）
5. ボタンの置き場所 `GonaviDevEx.mount(要素, () => そのページの名前の一覧)`

- **⚠ `mount()` は home.js の読み込みを待ってから置く**（`DOMContentLoaded`）。置いたあと、外しているポケモンがあれば
  `on()` を呼んで**一覧を作り直す**（最初の描画には間に合っていないため）
- **⚠ 候補の照合はひらがな・カタカナを両側そろえる**（片側だけカタカナに直すと「ケルディオ(かくご)」が探せない。実際に踏んだ）
- 見た目は紫（開発者用の色）・パネルは375pxでもはみ出さない

### 広告の枠 `assets/ads.css` / `assets/ads.js`（2026-09-21・タダシさん決定・審査の合格待ち）

**置くのは2か所だけ: A（`data-ad="top"`）＝タイトルのすぐ下／B（`data-ad="mid"`）＝ランキング（主な結果）の下**。

- **⚠ 操作の途中に広告を挟まない**（タイプ選択とランキングの間などは不採用。上であるほど目のピントは下の機能へ向かうので邪魔にならない）
- **⚠ 時間差でクイッと動く広告は絶対に作らない**: 枠の高さは ads.css で最初から固定（top 116px・mid 266px＝「広告」の札16px込み）。広告が来ても・来なくても・失敗しても下の中身を1pxも動かさない。来なかった枠も畳まない
- **いちばん下には置かない**（収益の見込みがいちばん低いのに、広告の数だけが増えるため）
- **自動広告（全面・下に貼り付く型）は使わない**。出すのは決めた枠だけ
- **`ads.js` の `CLIENT`・`SLOTS` が空のあいだは何も出ない**（合格後に入れる）。**開発者の端末（`GonaviDev()`）には出さない**
- **`?adtest=1`** で本物の代わりに同じ大きさの点線の箱が出る（そのタブだけ・`?adtest=0` で解除）。配置はこれで確かめる
- ページ側: `<head>` に `ads.css` と `ads.js`（**defer・async を付けない**＝描画の前に枠の有無を決める）、本文に `<div class="adslot" data-ad="top" data-snap-skip></div>`。**`data-snap-skip` を必ず付ける**（保存する画像に写さない）
- **ランキングの一覧の中・模擬戦の全画面・ボタンのすぐ隣・中身の少ないページ（404・引っ越し・フィードバック・`/feedback/`）には置かない**
- **ボスを選ぶまで結果が空のページは、B に `data-ad-when="#結果の要素"` を付ける**（`.empty` 以外の要素が入ってから枠を出す。空の画面に広告だけ置かない。出るのは結果の下なので上の中身は動かない）。付けてあるのは レイド火力 `#rankList`・レイドシミュ `#resBody`・ジム挑戦 `#ranklist`・マックスバトル対策 `#list`
- **B を置くのは「ランキング（順位の一覧）の下」だけ**。ランキングの無いページは A だけにする（いちばん下になるだけの枠は置かない、と同じ考え方）
- 入れ終えたページ（全ツール）: **A＋B**＝レイド火力（**template.html と dps の両方**）・レイドシミュ・タイプ別火力・ジム挑戦・ジム防衛・マックスバトル対策・マックスバトル タイプ別2枚・耐久指数・個体値チェッカー（B は TOP10 の結果の枠の下・`data-ad-when="#top10"`）／**Aだけ**＝ステータス図鑑・わざ図鑑・ブレイクポイント・GBL・ロケット団・対戦記録
- **GBL・ロケット団・対戦記録の A は `assets/gbl-app.js` の骨組み（`</header>` の直後）に1か所**。模擬戦の全画面（`.bfull`）は画面を覆うので広告は見えない。3つの sw.js と個体値チェッカーの sw.js の先読みに ads.css・ads.js を入れてある
- **トップページと案内ページ（運営者情報・プライバシー・フィードバック・引っ越し・404）には置かない**（トップはツールへの入口＝サイトの顔で、中身は一覧だけ。落ち着いた見た目の決まりを優先）
- **合格したらやること**: ①アドセンスの管理画面で広告ユニットを2つ作る（A用・B用・どちらも「ディスプレイ広告」・**自動広告はオフ**） ②`assets/ads.js` の `CLIENT`（`ca-pub-7623378724303874`）と `SLOTS` に番号を入れる ③プライバシーポリシー2節の「利用することがあります」を「利用しています」に直す ④ヨーロッパ向けの同意画面は管理画面の「プライバシーとメッセージ」で Google 提供のものをオンにする（ページ側の追加は不要） ⑤gbl・rocket・battlelog・iv-checker の sw.js の CACHE を上げる ⑥1か月後に枠ごとの収益を見て、稼がない枠を外す

### データの引っ越し `/backup/`（2026-09-11実装・公開前の作業B1）

**端末に保存した内容（対戦記録・★登録・パーティ・見せ合いの6匹・模擬戦のあいて・レイドの6匹編成・自由設定わざ・表示の設定）を書き出して、
別の端末やブラウザへ移すページ**（機種変更や**ホーム画面アプリとSafari（保存場所が別）の行き来**で消えるのを防ぐ）。見た目は `infopage.css`。

- **対象はこのサイトのキーだけ**（`gbl_`／`rkt_`／`raid_`／`site_`／`karyoku_` で始まるもの。`site_dev`＝開発者モードは除く）。
  **新しい保存キーはこの接頭辞のどれかを使う**（使わないと引っ越しから漏れる）
- 形式は `{app:'gonavi', v:1, t:日時, data:{キー: 保存されている文字列そのまま}}`（文字列のまま運ぶので各ツールの保存形式が変わっても壊れない）
- **書き出す**: 「ファイルに保存」（`gonavi-backup-年月日.json`。スマホは共有メニューへ＝ホーム画面アプリではダウンロードが効かないことがあるため。
  断られたら通常のダウンロード）／「文字でコピー」（クリップボード。使えなければ文字欄を出して手でコピー）
- **読み込む**: ファイルを選ぶ／貼り付け。件数を見せてから2択:
  **足し合わせる**＝対戦記録（idで重複を除き日時順）・★登録（同じ内容は1つ・30件まで）・自由設定わざ（idで重複除去）は合わせ、
  **そのほかはこの端末に無いものだけ入れる**／**まるごと置き換える**＝確認ウィンドウのあと、対象キーを消して読み込むデータにそろえる。
  形の違う文字は拒否。`site_dev` と他用途のキーは触らない
- 入口: 全ページ最下部のリンク（`home.js` の `footLinks`・トップページのフッター）と、**対戦記録の履歴タブ（全削除ボタンの上）**の「💾 記録を書き出す・読み込む」。
  プライバシーポリシーにも「書き出したファイルは端末の中にだけ作られる」と記載

### 見せ合いの6匹をまるごと保存・呼び出し（2026-09-11実装・公開前の作業B2）

**何度も当たる6匹の顔ぶれを、わざ構成ごと名前を付けて取っておく**（★登録は1匹ずつのため）。
見せ合いの6枠の下（じぶん側・あいて側それぞれ）に「📋 保存した6匹 N」のボタン（`renderSdSave`・`gbl-app.js`）。

- 保存先は **`gbl_sd_teams`**（`{id, side:'my'|'foe', cap, name, t, mons:[6枠そのまま]}` の配列）。**じぶん側とあいて側で別の一覧**・各30件まで
  （超えたら保存を断って「消してから」と出す。黙って古いものを消さない）。`gbl_` なので「データの引っ越し」に自動で含まれる
- 開くと **名前の入力欄（16px・空ならリーグ名＋日付）＋「いまの6匹を保存」** と一覧（名前・保存したリーグ・6匹の名前）。
  **「入れる」＝その6匹をわざ構成ごと枠へ**（じぶん側なら選出はいったん外す）／**×→「削除する?」の2タップで削除**（対戦記録と同じ流儀）
- 呼び出しは `sdtRestore`: データから消えた・戦えないポケモンは空枠、消えたわざは定番構成で埋める。
  重複・メガ2匹は `renderSd` の `sdDedupe` が掃除する。**別リーグで保存したものも入れられる**（理想個体値でそのリーグに合わせ直す）
- 保存名は利用者が打つ文字なので **必ず `escH()` を通して表示**する

### 名前の検索欄の守り（全ツール共通・2026-09-10タダシさん報告で新設・恒久）

候補をタップせずにキーボードの「✓」「完了」で入力を終えると、入力欄は打った名前・中身は前のポケモンのまま・候補も開いたまま、
という食い違いを防ぐ。**全ページが読む `home.js` の `suggGuard()` 1か所で守る**（ページ側の追加は不要）:

- **候補を選ばずに入力を終えたら**、名前が完全に一致する候補（無ければ候補が1つだけのとき）を選んだことにする
- **どれとも決まらなければ、入力欄を入力前の名前に戻して候補を閉じる**（入力欄と中身を絶対に食い違わせない）。空にしたときはページ側の「消す」を尊重
- **どの候補にも当たらない文字でも守る**（名前の欄は常に守る）。数字の欄（`inputmode` が numeric/decimal・`type="number"`）は対象外
- **改行キー**でも候補を選べる（一致する候補、無ければいちばん上）。日本語の変換の確定（isComposing・keyCode 229）は除く
- **パソコンで候補を押したとき入力欄のフォーカスを外さない**（mousedown を止める。スクロールは妨げない）
- **候補を押したら・「完了/改行」を押したらキーボードを閉じる**（`suggGuard` が入力欄から離す。iPhoneでは離れずキーボードと連絡先の自動入力バーが残るため）
- 対象は「**`.sugg` または `.searchbox` の中の入力欄**」と、その中の候補一覧（`.sugg-list`／`#sugg`／`#suggest`）。
  **候補は一覧の直下の要素**、選べないものは `class="dup"`（または `sep`・`disabled`）。
  **⚠ 新しく名前の検索欄を作るときも必ずこの形にそろえる**（そろえれば自動で守りが効く）
- **⚠ 候補1つを見分ける条件は `LIST + ' > *'` と書かない**（カンマ区切りの最後にしか効かず、候補を押しても入力欄を離れた瞬間に元の名前へ戻る。いまは `ITEM` を別に作ってある）
- 見せ合いの登録カードは、候補を選んだら入力を終える（`.editing` の拡大を解く）・入力欄の文字も選んだ名前にそろえる
- **⚠ 変換中も候補を出す**（iPhoneの日本語入力は確定まで `isComposing` のままなので、変換中に何もしないと候補が出ない）。
  ルールは **「変換中も候補は出す・入力欄の文字を書き換えるのは変換していないときだけ」**（変換中に書き換えると変換が壊れる）。
  照合は `toKata` を通す（ひらがなでも当たる）。**新しく名前の検索欄を作るときも必ずこの形にする**
- **iPhoneの入力補助を止める**（`autocomplete/autocorrect/autocapitalize=off`・`spellcheck=false`・`enterkeyhint=done`）。
  **付けるのは読み込み時とあとから作られた欄**（MutationObserver）で、**ふれた瞬間では遅い**。
  入力欄の説明文の「名前」は「ポケモン」に置き換える（人の名前の欄と判断されて連絡先の自動入力が出るため・効果は実機で要確認）
- **iPhoneの実機・シミュレーターでは未確認**（このMacはXcode未導入）
- **⚠ 一覧の外から名前を入れたときは戻さない**（2026-09-14修正）: 裏読み・よく出る・最近のパーティ・★登録リストなど**一覧の外のボタン**で入れた名前を、
  フォーカスが外れた0.2秒後に入力前の名前（空欄）へ戻していた。いまは**入力欄の文字が自分で打った文字（`_sgTyped`・ふれてから打っていなければ入力前の文字）のままのときだけ**守り、
  ページ側が別の名前を入れていたらそちらを尊重する。**この形なら一覧の外のボタンを新しく作っても自動で守られる**

### 未完了・保留の予定（2026-09-17時点・日ごとの作業まとめから集約）

完了したら行ごと消す。詳しくはメモリの各ファイル。

- **GBLバトルルールのマニュアルづくり**（2026-09-22開始）: 下書きは `.claude/drafts/gbl-battle-rules-draft.md`（確定まで正本にしない）。自動検査は `rules-scn`・`rules-random` として保存前に流れる（済）。残り＝下書きの **H節の候補（H1〜H6・H7ギルガルド・H10）と「？」の項目（C8）をタダシさんが精査**・リアルタイム操作と見せ合いの検査。「SPのミニゲームの出来で威力が変わる」は2026-09-22に実装済み（`.claude/rules/gbl.md` の「威力調整」の節・メーターの見た目5案はタダシさんの選択待ち）
- **アドセンスは2026-09-22に「有用性の低いコンテンツ」で不合格**。再申請の準備は3つ（タダシさん指示「①②③を全部進める」）: ②1ポケモン1ページの公開（2026-09-22にコミット済み・**push後にリポジトリの変数 `POKEPAGE` を `on` にし、Search Console に `sitemap-pokemon.xml` を送る**）／③各ツールのタイトル直下の紹介文（2026-09-22完了）／①読み物 `/guide/`（一覧＋初稿「個体値の順位とは」を2026-09-22に作成・**タダシさんの確認待ち**。計画は `.claude/drafts/guide-plan.md` の12本）。3つがそろったら再申請。広告の枠は全ツールに入れ終えた（IDが空なので何も出ない）。合格したらやることは「広告の枠」の節の手順どおり
- **英語版はアドセンスの審査が終わってから**。英文はClaudeが書く。ゲーム内の公式英語表記にそろえた用語集を1ファイルにまとめ、変更ごとに日本語の対訳を添える。`gonavi.jp/en/` のような別アドレスにするかは着手時に確認
- **テスター#20「モバイルの操作しづらさ」（iPhone・GBLと見せ合い）**: 原因が特定できていない・タダシさんの判断待ち。提案した3案＝①テスターに画面収録をもらう ②テスターに場面を聞き返す ③Claudeが390×699で通しで触って洗い出す
- **レイドシミュのタイムラインとUIの改善**（2026-09-13指定）
- **テスター期間終了のDM**（文面は作成済み）。募集の帯の期限（`feedback.js` の `UNTIL`）は10/31のまま・公開日の前日に止める
- **保留: 通常とシャドウの行まとめ（テスター#14）**。同じ声がまた来たらA案（副行・順位はそのまま）を提示
- **暫定: スーパーメガのシールド防御4倍**。新しい情報なし。収益化の前に「倍率はどうしますか？」と一言確認する
- ローカル確認用の `battlelog/_demo.html`（100戦の見本を入れて対戦記録を開く）は `.git/info/exclude` で管理外・公開しない

> **GBL・ロケット団・対戦記録・模擬戦の決まりは `.claude/rules/gbl.md` に移しました**（2026-09-17）。
> `assets/gbl-app.js`・`assets/pvp-engine.js`・`gbl/`・`rocket/`・`battlelog/` などを開くと自動で読み込まれます。

**⚠ タイトルの左のモンスターボールは `assets/pageheader.css` の1か所（2026-09-20・全ページ共通）**:
ボールは `header h1::before`（inline-block＋`margin-right:.4em`）。**h1 を flex にしない**——flex にすると同じ余白（column-gap）が語と語の間にも入り、「タイプ別火力｜ランキング」のような見出しが開いてしまう。
**⚠ すでに h1 が flex＋gap を持つページ**（レイド火力・レイドシミュ・ジム系・マックスバトル・GBL系）は、その gap がボールの右にも効いて**二重の余白になる**ので、ページ側で `margin-right:0` を当てて打ち消す。
**⚠ `assets/gbl.css` は pageheader.css より先に読み込まれる**ので、打ち消しは `body header h1::before` と詳細度を1つ上げて書く（同じ詳細度だと共通側が勝ってボールが離れる・実際に踏んだ）。
基準（390px幅でのボールと文字のすき間）: レイド火力9.3px／ジム系・マックスバトル10.0px／GBL系7.2px／そのほか8.2px

### 全ツール共通の「🏠 ツール一覧」（2026-09-01・共通ファイル）

`assets/home.css` / `assets/home.js`。theme・explain と同じ自己完結の共通部品で、**`#themesw` の先頭に🏠のピルを差し込む**。ページ側の追加は2行（新ツールも同じ）:

1. `<head>` に `home.css`（explain.css の直後）
2. **`explain.js` の直後**に `home.js`（theme.js が `#themesw` を innerHTML で書き換えるので必ずその後。並びは 🏠→💡→テーマ）

- **トップページでは出さない**（`location.pathname` で判定）
- セレクタは必ず **`#themesw .homesw`**（`.themesw button` に詳細度で勝つため。explain と同じ落とし穴）
- 430px以下は文字を隠して🏠だけ
- SWの先読みに追加済み（gbl-v49 / rkt-v44 / blg-v28 / ivc-v13）
- **⚠ 文字色は💡説明ボタンと同じ**（ダーク`#9fb0e0`/ライト`#5b6b96`・OFF状態の色。旧: 金 `#ffd873`/`#8a6000`）。hoverの色変化は廃止（影の変化だけ）。focus-visibleの輪郭は`#8b5cf6`。`home.css`1か所で全ツールに効く

**ルートのツール名は2026-08-28に「火力チェッカー」→「レイド火力チェッカー」へ改名**（タダシさん指示。**このメモの「火力チェッカー」はすべて同じツール**）。
title・見出し・manifest（short_nameは「レイド火力」）・/raid/ のリンクと使い方を変更。アイコン・URL・localStorageキー（`karyoku_theme`等）はそのまま。

## 確定仕様（ユーザー承認済み・変更禁止）

以下はユーザーが実測検証のうえ確定した仕様。リファクタや機能追加で壊さないこと。
変更が必要になった場合は必ずユーザーに確認を取る。

### 全12ツールの総ざらい（2026-09-06）

**直した不具合**:

- レイド火力チェッカーの最少討伐人数の一文で名前が二重（「**メガ**メガマフォクシー」）。原因は「メガ」マーク（`badges()`）を文中にも入れていたこと
  → **`mp-sub` の一文だけマークを出さない**（`badges(shadow, false, p)`）。ランキングの行・見出しはバッジのまま
  **⚠ `template.html` と `dps/index.html` の両方を直す**（片方だけだと毎朝の自動更新で消える）

**紛らわしいが不具合ではないもの**:

- レイドシミュのタイムラインの表が画面幅を超える → `.tlwrap`（`overflow-x:auto`）内のスクロール。ページは横スクロールしない
- シャドウの絞り込みボタンに文字が無い → 紫の炎マークの恒久ルールどおり（`aria-label` と `title` あり）
- ジム挑戦の「反撃技タイプ:」の後ろが空に見える → タイプアイコンで出しているだけ
- 個体値チェッカーでハイパーとマスターの順位が同じ → CP2500に収まる個体は両リーグで同じ上限PLまで上げられるので正しい

### メガ・ゲンシはどんな場合でもパーティに1匹まで（2026-09-05確定・タダシさん指示・恒久ルール）

**ゲームのルールなので、シミュレーターの編成の枠すべてに効かせる**。判定は `isMega(key)`（ゲンシ＝`_primal` も含む）。

- **共通ヘルパー `megaOver(arr, key, skip)`（`isMega` の直後）**。`suggRow(k, ng)` で入れられない候補を**消さずにグレーにして理由を出す**（消すと壊れて見える。同じポケモンの禁止と同じ流儀）
- **効かせる場所**: じぶん3枠（`PT`＝パーティ診断・GBL模擬戦・ロケット団模擬戦共通）／模擬戦のあいて3枠（`GBT`）／見せ合いの6枠（`SD.my`・`SD.foe`）／対戦記録の入力3枠（`BLE.foes`）／各**★登録リストから選ぶとき**／**🎲おまかせ3匹・6匹**／**AIの選出（`sdFoePick`・EASYの「上から3匹」・予備の道も含む）**／**パーティ診断の入れ替え候補**（他の枠にメガがいればメガを提案しない）
- **復元時の掃除**: 起動時の `dropUnknownPk()` が2匹目以降のメガを落とす（`S`・`PT`・`RKT`・`RBM`・`GBM`・`GBT`・`SD`・`BLE.foes`）。**見せ合いは `sdDedupe()` が描き直しのたびに掃除**（共有リンク `sdf=` の復元が後から走るため）
- **⚠ 新しく編成の枠を作ったら必ずこのルールを通す**（1対1シミュの左右は対象外）
- **⚠ 予備の道にも効かせる**: `sdBestCombo` の「合う組み合わせが無い」ときの `pool.slice(0,3)` でメガ2匹を選出していた。予備の道も上から順にルールを守って取る


### 全ツール共通のヘッダー配置(2026-09-04確定・タダシさん指摘)

原因だった2点（再発防止）: ①`header`が`flex-wrap:nowrap`（未指定含む）でタイトルとボタンが重なる・はみ出す ②`#themesw`を`header`の外やタイトルより前に置く。

**共通ファイル `assets/pageheader.css` に一元化**（theme.css/explain.css/home.css/tabs.css/wordmark.cssと同じ自己完結の流儀）。全ツールでこの並び（恒久ルール）:

1. **⚠ 2026-09-11タダシさん指示: `.pagelink`（左）と `#themesw`（🏠→📖→🌙☀️・右寄せ）の1行はヘッダーのいちばん上（英字の小見出しより上）**。タイトルの下だと主役のボタンの直前に割り込むため（トップページの上の細いバーと同じ考え方）。**HTMLの順番は変えず `pageheader.css` の `order`（.pagelink=-2・#themesw=-1）で入れ替える**
2. **タイトル（小見出し・見出し・添え書き）はその下で、常に行いっぱい**（`header>*{flex:1 1 100%}`）
3. **`.pagelink` と `#themesw` は必ず header の直下の子**（入れ子だと order が効かない。レイドシミュは `.hmain` の中から外へ出した）

ページ側がやること（新ツールも同じ）:

1. `<head>` に `<link rel="stylesheet" href="/assets/pageheader.css">`（home.cssの直後）
2. `<header>` の子は「見出し」→（あれば`.pagelink`）→`#themesw` の順。`#themesw`をheaderの外や先頭に置かない

- 各ページの`<style>`の header直下のflex配置（display/flex-wrap/align-items/justify-content/gap）と、`#themesw`・`.themesw`の位置指定（`width:max-content;margin:...auto`等）は削除済み（二重管理・競合を避ける。ページ固有の見た目だけ残す）
- `/gbl/`・`/rocket/`・`/battlelog/`は`assets/gbl.css`の重複するheaderのflexルールを削除。`header.haslink`クラスも不要になり削除
- **`/dps/`は`template.html`も直す**（`build_data.py`が`template.html`から`dps/index.html`を再生成するため）
- 個体値チェッカーは見出しの中央寄せ（`text-align:center`）を残し、`#themesw`の位置だけ直した

### UIデザインの共通ルール（2026-08-10確定・ユーザー承認済み／全ツールへ展開する）

**/gbl/ と /rocket/ のタブ類はこの形**。他ツールのUI改善でも同じ質感でそろえる。

- **立体的なボタン**: 背景 `linear-gradient(180deg,#1e2850,#151d38)`、`box-shadow:0 2〜3px 0 rgba(0,0,0,.4)` で浮かせ、`:hover` と選択中に `translateY(-1px)`
- **選択中は役割の色のグラデーションで点灯**: `linear-gradient(160deg, 明るい色 0%, 主色 45%, 濃い色 100%)` ＋ `inset 0 1px 0 rgba(255,255,255,.6)`（内側のハイライト）＋ 1px持ち上げ
- **⚠ 点灯の色つきのにじみ（光彩）は全ツールで廃止**（2026-09-03タダシさん指示。旧: `box-shadow: 0 5〜6px 16〜18px rgba(主色,.4)`）。**動画用スクショにぼやぼやが写り込む**ため。
  立体感は内側のハイライト・持ち上げ・グラデーションの面だけで出す。外したのは点灯中のタブ・チップと1〜3位の順位タグの外側のにじみ。**立体の黒い影・ぼかし0のリング・内側のハイライトは残す**。
  対象は共通の `assets/tabs.css`・`mega-lv.css`・`theme.css`・`explain.css` と各ツールの同じ規則。
  **残す光は2つだけ**: 模擬戦の主役ボタン（`.mockhero`）と、SPゲージが撃てるときの光（`.gq.on`）。**新しいタブ・チップにも光彩を付けない**
- **未選択でもその色を文字色で（薄く）示す**
- **役割ごとの色分け**（確定）:
  | 場所 | 色 |
  |---|---|
  | GBLリーグ: スーパー / ハイパー / マスター / 特殊カップ | 濃い青(文字白) / 黄 / 紫 / 白っぽいグレー |
  | GBLモードタブ（1対1シミュ等） | **薄い水色青**（リーグの濃い青と見分けるため） |
  | GBLの「ロケット団戦↗」リンク | 赤系（別ページへ飛ぶことを色で示す） |
  | ロケット団のモード切替: 1対1モード / 模擬戦モード | 水色 / **GBLと同じ主役ボタン `.mockhero`**（2026-08-23タダシさん指示。1対1は1行目・模擬戦は2行目に横いっぱい） |
  | ロケット団のあいて種別: したっぱ→リーダー→サカキ | 赤系（強敵になるほど濃く） |
  | ロケット団の「だれと」・手持ちチップ | 金系 |
  | 火力チェッカーの絞り込み: シャドウ / メガ・ゲンシ / 🛡シールド破壊 | 紫 / マゼンタ（名前横のメガマークと同系） / 水色 |
  | 火力チェッカーの計算のしかた: 今の状態 / フル強化 | 水色（STEP2のカードの色。排他の2択なのでくっつけて1つのまとまりに見せる） |
- **全ツールがダークテーマ**。タブの土台 `linear-gradient(180deg,#1e2850,#151d38)`、影 `0 2px 0 rgba(0,0,0,.4)` ＋ `inset 0 1px 0 rgba(255,255,255,.06)`。**未選択の文字色は暗い土台でも読める明るめの同系色**
- **明るい色で点灯するタブは文字を濃色に**: 水色（`#08b4c9`系）やゲンシの明るいグラデーションは `#042330`。紫・マゼンタは白のまま。明るいグラデーションのボタン（「ランキングに追加」）も濃色
- **排他の2択と独立ON/OFFは並びで区別**: 排他は**すき間2pxでくっつけ**外側だけ角丸を大きく（`.seg`）。独立ON/OFFは**離して置く**
- **タブの幅**: 広い画面は**内容ぶんの幅で左寄せ**、狭い画面（560px以下）は `flex:1 1 auto` で**横いっぱい**

### 火力チェッカー（ルート）のUI見直し（2026-08-14実装）

**画面には結論だけ、仕様・前提の説明はページ最下部の「使い方」タブへ集約**（恒久ルール）。

- **`#helptab`／`#helpbody`**（/gbl/ と同じ作り）。中身は**火力（DPS）の意味／わざの選ばれ方／自分のポケモンの登録／🛡シールド破壊**の4節（ランキング下の注記を全部移した）
- **絞り込みタブは立体グラデーション＋役割色**（上の表）。「シールド破壊(メガ)」→**「🛡シールド破壊」**（メガ専用は押したあとの注記と使い方タブで示す）
- **STEP2はタブ**: **「今の状態｜フル強化」の2択タブ**（フル強化＝PL50・個体値15）＋**「シャドウ」トグル**。状態は `myMode`／`myShadowOn`
- **個体値の欄は見出し「個体値」＋「攻撃／防御／HP」**
- **わざ構成は切らずにわざ単位で折り返す**（`.rmoves.mvline`＋`.mv`。nowrapだと「シャドー…」と切れて読めない）。狭い画面の余白: カード11px・行3px・順位30px・DPS64px・わざ.72rem
- **自分の登録欄（`.mine-sum`）は「あなたの」なし、シャドウ・メガは名前の左のマーク**（`badges()`）
- **言い回し**: 「レイドボスを選ぶ」／「自分のポケモン（任意）」／「ボスのわざ」／空状態「ボスを選ぶとランキングが出ます」
- シールド破壊の自分のメガが耐えられないときの文言は `plMaxOf(e)` で上限PLを出す（「PL51」固定は誤り。メガLv4は52・53）

**ダークテーマ化（2026-08-14・タダシさん指示）**:

土台は /gbl/ と同じ濃紺（`#0b1023`〜`#1a1024` のグラデーション＋水色・紫・オレンジのにじみ）。**役割色（レイド=赤〜オレンジ／自分=水色／ランキング=紫）は残す**。

- カードは `rgba(26,35,66,.94)` の面＋役割色の細い枠と落ち影、上端に役割色のグラデーションバー
- **メダルの3行は暗い土台に薄く色を敷く**（`rgba(255,213,74,.16)`→透明＋同系の枠＋順位の数字を金・銀・銅）。ベタ塗りは浮きすぎる
- 自分の登録欄（`.mine-sum`）は**金を薄く敷き、左端5pxの金の帯**
- **タイプアイコンの縁取りは全タイプへ**（`.tbadge`。暗い背景ではあく等の暗いタイプが溶ける）
- `theme-color` は `#0b1023`

**ダーク⇄ライトの切り替え（2026-08-14・タダシさん指示）**:

画面の右上で切り替え（ライトは白ベース版を復活させたもの）。

- **色はすべて `:root`（ダーク＝既定）と `:root.light` の2ブロックだけで決める**。個別のルールに色を直接書かない。カードの役割色（`.c-boss`/`.c-my`/`.c-rank`）だけ `:root.light .c-boss{...}` の形で上書き
- **既定はダーク**。**OSの設定（`prefers-color-scheme`）には従わない**（他ツールと食い違うため）
- 選んだテーマは `localStorage` の `karyoku_theme`。**`<head>` のインラインscriptで描画前に `class="light"` を当てる**（後からだとダークが一瞬光る）。`theme-color` も切り替え時に差し替え
- スイッチは**🌙／☀️ の2択タブ**（`.themesw`・月＝紺・太陽＝金）。**見出しは「ポケモンGO」「火力チェッカー」の切れ目で折り返す**
**全ツールへ展開（2026-08-14・タダシさん指示）**:

全ページに同じスイッチ。**部品は共通ファイル1組**なので新ツールも下の3行を足すだけ。

- **`assets/theme.css`**（見た目。**各ツールのCSS変数に依存せず自己完結**）／**`assets/theme.js`**（生成・保存・切り替え）
- ページ側:
  1. `<head>` に `theme.css` ＋ **早期適用のインラインscript**（`localStorage.getItem('site_theme')==='light'` なら `document.documentElement.className='light'`）
  2. 右上に `<div id="themesw"></div>`
  3. 末尾で `theme.js`
- **保存キーは全ツール共通の `site_theme`**（どのツールへ移っても同じ見た目）
- **アドレスバーの色は各ツールの `--tcolor`**（theme.js が切り替え時に読む）
- **色はどのツールも `:root` と `:root.light` の2ブロックに集約**。個別のルールに色を直接書かない
- **⚠ ライト化の落とし穴**:
  - **明るい色で点灯するタブ・バッジの文字色は変数に**（`--onaccent`／`--onml`／`--ongold` など）。ライトは点灯色が濃くなるため
  - **未選択のタブの文字色もテーマごとに**（GBLのリーグタブ・ロケット団の種別タブ。黄・白っぽいグレーは白い土台で見えない）
  - **暗い面を直接書いている箇所**（タブの土台・バーの奥・ロケット団のリンク）は変数へ
  - **タイプアイコンの縁取りは向きが逆**（ライトでは明るいタイプが溶ける）
  - **見出しは語の切れ目で折り返す**: `header` を `flex-wrap:nowrap` にして見出し側を縮め、`<span>`/`<b>` に `white-space:nowrap`
  - **例外: /rocket/ は560px以下でヘッダーを2段**（右の「GBL対面シミュ ↗」(123px)で幅が足りず、見出しを縮めても直らないため）。`header.haslink` 限定で `flex-wrap:wrap` ＋ `h1{flex:1 1 100%}`。/gbl/ と広い画面は1段（※ `header.haslink` は2026-09-04のヘッダー配置の統一で削除済み）
- **`gbl/sw.js`・`rocket/sw.js`・`iv-checker/sw.js` の先読みに theme.css / theme.js を追加**（gbl-v8 / rkt-v3 / ivc-v3）

**ライトは白一色にしない（2026-08-14確定・タダシさん指示）**:

単色の背景＋純白のカードは「白すぎる」と却下。

- **背景は `--bgimg` にまとめ、ツールの役割色を淡くにじませる**（`radial-gradient` 3枚＋斜めの `linear-gradient`）。`background-attachment:fixed`。ダークも同じ変数で切り替える
- **色の組み合わせ**: 火力チェッカー=水色・紫・オレンジ ／ 個体値チェッカーとGBL=青・紫・水色 ／ ジム挑戦=黄・水色 ／ ジム防衛=緑・水色 ／ マックスバトル=ピンク・紫
- **カードは半透明**（`rgba(255,255,255,.88)` 前後）
- **カードに淡い色の落ち影**（`--cardsh`・役割色を混ぜる）。ダーク側の `--cardsh` は `none`

### 説明ありモード(💡)は共通ファイル `assets/explain.css` / `assets/explain.js`（2026-08-15実装）

画面から外した補足（`class="expl"` で隠したもの）を**右上の💡ボタンで一斉に出し入れする**。自己完結の共通部品。

- **ボタンは explain.js が `#themesw` の先頭に差し込む**（HTML追加不要）。ページ側は3行（新ツールも同じ）:
  1. `<head>` に `explain.css`
  2. **テーマの早期適用scriptの直後**に
     `<script>try{if(localStorage.getItem('site_explain')==='1')document.documentElement.classList.add('explain');}catch(e){}</script>`
     （テーマ側が `className='light'` で丸ごと上書きするため**必ずテーマの後**）
  3. **`theme.js` の直後**に `explain.js`（先だとテーマ側の `innerHTML` でボタンが消える）
- **見た目は消灯した電球のピル型ボタン**（💡＋「説明」）。OFFは色を抜いて消灯、**ONで水色のグラデーションに点灯**して弾む（`ebpop`）。430px以下は💡だけ
- **保存キーは全ツール共通の `site_explain`**。既定はOFF
- **⚠ セレクタは必ず `#themesw .explainsw`**。素の `.explainsw` だと `:root.light .themesw button` に負けて**ライトだけ点灯しない**。ライトのOFF土台は `:not([aria-pressed="true"])` で限定する
- SWの先読みに追加済み（gbl-v10 / rkt-v5 / ivc-v7）
- **補足は全ツールの主なタブ行の直下に** `class="enote expl"` の1〜2文（火力チェッカー＝一覧の前提／ジム挑戦＝火力と総合力の違い・PL前提／ジム防衛＝総合の中身と二重弱点／マックスバトル＝アタッカーとタンクの違い／レイド＝ボスSPの3択と再突入／個体値チェッカー＝並び順とPL上限／GBL一覧系3モード＝相手の前提・🛡の列・環境スコア・残◯%／ブレイクポイント＝各パネルの前提）。**書き方**: 「A＝…。B＝…」で言い切る・専門語に短い意味・2文まで。文字を減らす修正のたびに `class="expl"` で残す

**長押し照準の説明ウィンドウ（2026-08-15実装・タダシさん発案の操作）**:

💡ONのあいだ、**説明を持つ部品（`title` か `data-help`）を長押しで照準モード**。指の位置の部品が水色に光り、説明ウィンドウ（`#exptip`）が指の上に出る。**スライドで照準が移りウィンドウが差し替わる**。離すと残り、次のタップで閉じる。パソコンはホバーで同じ。実装は `explain.js`/`explain.css`。

- **説明文は既存の `title` を使う**。新しく書くときは `title` でも `data-help` でもよい
- **タイプアイコン(.tbadge)は対象外**（タダシさん指示）。`title` はライトのCSS（`[title="でんき"]` 等）と読み上げ用に残し、対象選別と点線の印からだけ外す。個別に外すなら **`class="noexp"`**
- **全ツールのタブとランキング行の数字・マークに説明あり**。説明文の文体は「〜します」。火力チェッカーは文言を `T_DPS` 等の定数にまとめる
- **⚠ 同じチップを複数の分岐で生成している場所は全部直す**（マックスバトルのアタッカーは `r.fast`/`r.rec` の2分岐が同じ0.5秒チップを作る）
- 「耐久指数＝HP種族値×防御種族値」の常時表示は `class="expl"` へ（説明で足りる文は通常表示から消す方針）
- 💡ONのあいだ、説明を持つ部品に**点線の印**（タダシさん選択の案A。スイッチ自身には付けない）
- 挙動: 長押し判定0.4秒（iOSの文字選択より先に取る）／12px以上動いたら取り消し／**照準中はスクロールと文字選択を止める**（touchmove の preventDefault ＋ user-select none）／**離した直後のclickは1回握りつぶす**（タブの誤発動防止）／対象外にいるあいだは最後のウィンドウを出したまま
- `title` は最初に触れた時点で `data-help` へ移す（二重表示防止）。**💡OFFで `title` へ戻す**
- **実機のiOSでの長押し（文字選択との競合）はタダシさんの実機確認待ち**

### タブと「使い方」タブは共通ファイル `assets/tabs.css`（2026-08-14確定）

タブの質感と最下部の「使い方」タブを1か所に。自己完結なので新ツールは `<link rel="stylesheet" href="/assets/tabs.css">` の1行でよい。

- **並び**: 独立ON/OFFは `<div class="gtabs">` に直接ボタン／排他の2〜3択は `<div class="gtabs"><div class="gseg">…</div></div>`。ボタン1つの並びは **`.gtabs.one`**（狭い画面で横いっぱいにしない）
- **色はプリセットのクラス**: `t-shadow`(紫) / `t-mega`(マゼンタ) / `t-cyan`(水色) / `t-gold`(金) / `t-green`(緑) / `t-blue`(青) / `t-red`(赤)。
  自分で決めるときは `--mc`(ダークの未選択の文字色) / `--mcl`(ライトの未選択の文字色) / `--mg`(点灯のグラデーション) / `--mgl`(光彩) / `--mi`(点灯の文字色・既定は白)
- **点灯の判定は `aria-pressed="true"` / `aria-selected="true"`**。**⚠ ライトの未選択は `:not([aria-pressed="true"]):not([aria-selected="true"])` で限定する**（しないとライトだけ点灯しない）
- **使い方タブは `.helptab` / `.helpbody`**。文字色はツール側の変数（`--sub`/`--line`/`--panel`/`--text` か `--ink`）
- **使い方タブの余白は全ツール同じ**（2026-08-23タダシさん指示）: 段落のあと12px・箇条書きは項目ごとに9px・h4は上に区切り線＋26px・行間1.9・文字.76rem。
  **定義は4か所（`tabs.css`／`gbl.css`の`#helpbody`／`template.html`／`raid/index.html`）。変えるときは4つそろえる**
- **名前の頭のシャドウマーク `.shadowmark.nmk` は `assets/shadow-icon.css`**（全ツール共通）

### ジム挑戦・ジム防衛・個体値チェッカー・マックスバトルのUI見直し（2026-08-14実装）

共通ルールにそろえ、**説明はすべて最下部の「使い方」タブへ**。

- **ジム挑戦**: 「**火力｜総合力**」の2択タブ（金）＋絞り込みはシャドウ(紫)・メガ・ゲンシ(マゼンタ)・**同じポケモン表示（わざ違い）**(水色)。
  最後のボタンは**2行組み**（「同じポケモン表示」＋小さく「（わざ違い）」）。「わざ違い」だけでは分からないとタダシさん指摘＝**文字を減らすより、ぱっと見で分かるを優先**。
  2行のボタンが混ざる並びは `align-items:stretch`。**上位3件にメダルの色**、**ポイントの単位は「ポイント」だけ**。シャドウは名前の左のマーク。詳細設定は閉じているあいだ点線の枠だけ
- **ジム防衛**: 「**基本｜迎撃込み**」の2択タブ（緑）＋「**二重弱点も**」(赤・`.gtabs.one`)。プレースホルダーは「ポケモン名で検索」
- **個体値チェッカー**: リーグは**スーパー=濃い青／ハイパー=黄／マスター=紫**、並び順・PL上限・入手方法は水色。**選択中を `.act` で表す**ので共通CSSではなくページ内で同じ見た目を作る。
  **点灯中のタブでは順位の数字もタブの文字色に**（`.lgc.act .lgc-r{color:inherit!important}`）
- **マックスバトル**: 絞り込みは共通タブ（SP判定=金／**特別技も**=水色／**0.5秒技だけ**=水色／**交代受け**=紫）。★5・★6は金の2択タブ。「アタッカー｜タンク」の大きなタブは主役なので従来のまま
- **ダークの背景も役割色をにじませる**（ジム挑戦=黄・水色・オレンジ・紫／ジム防衛=緑・水色。単色 `#101418` は沈む）
- **ダークの土台は黒にしない**（タダシさん指摘）: ジム挑戦は**紺〜すみれ**（`#111a2e`→`#151b30`→`#1d1730`）、ジム防衛は**深い青緑**（`#0e1f26`→`#12202c`→`#14251f`）、にじみは `.10` 前後 → **`.15〜.22`**。
  **カードの面と枠も土台の色みに寄せる**。**下側にも大きめのにじみを2枚**。`--tcolor` も土台に合わせる
- **順位マークは1位=金・2位=銀・3位=銅、4位以降は色なし**（`rankClass`。旧「2〜10位=銀／11〜100位=銅」は他ツールとそろわないため廃止）

### 上位3件の見せ方は全ツール共通（2026-08-15確定・タダシさん指示）

**順位のタグ＋フレームのアウトライン**を金・銀・銅で必ずセットに。基準は**マックスバトルの1位**。ジム挑戦・ジム防衛・マックスバトル・火力チェッカーでそろえる。

- **順位はタグで出す**（文字色だけにしない）
- **タグの面**: `linear-gradient(160deg, 明るい色 0%, 主色 55%, 濃い色 100%)` ＋ `inset 0 1px 0 rgba(255,255,255,.5)`。文字は**濃色**（`--ongold` 等）。**タグの色つきの落ち影は2026-09-03に廃止**（マックスバトルの名前プレートの色影も）
- **フレーム**: 枠線をメダルの色に（旧: `0 0 22px その色の光彩`＋淡い色みの敷き）
  - **光彩と敷きは全ツールで廃止**（タダシさん指摘: ブラーで視認性が落ちる）。中の面は通常の行と同じ、メダル色は**くっきりした枠線＋左端4pxの帯（メダルと同じグラデーション）**。
    マックスバトルは1pxのグラデーション枠の内側に帯（`::after`）。ジム挑戦の順位の列は50px（狭い画面42px。「10位」を折らない）
- **マックスバトルだけは1pxのグラデーション枠**（`::before` のマスク）。1位は金→ピンク→紫、**2位は銀→紫**
- **3位はピンクを使わない**（1位と同じ暖色に見えるため）。**暗い銅→焦茶→くすんだ赤紫**（`--f3a/--f3b/--f3c`）。タグの銅色はそのまま（枠だけ専用色）。名前プレートも同じ考え方
- **4位以降は色を付けない**（マックスバトルも1/2/3だけ）
- **火力チェッカー**:
  - **4位以降のタグの面は `--rkbg`**（ダーク `#1a2342` ／ ライト `#edf1f9`）。`--chip` はライトで純白でタグが消えるため
  - **メダルの色は `--mgold/--msilver/--mbronze`**。`--gold` は文字色用で、ライトでは `#b07a00` と暗すぎる
  - **狭い画面ではタグを詰める**（`min-width:34px`・`.72rem`・`padding:3px 2px`。わざ構成の折り返しを抑える）
  - 順位の表記は**すべて「◯位」**
- **個体値チェッカーだけは順位に金・銀・銅を付けない**（タダシさん指示・専用セクション参照）
- **行の中身は左右を中央でそろえる**（`.top{align-items:center}`。上そろえだとマックスバトルのタンクで左下が空く）。行の高さは右のポイント表示（数字＋単位の2行＝54px）が決める
- 画面から外した補足は**消さずに `class="expl"` で隠す**（恒久ルール）

### 2026-09-04にまとめて確定した表記・表示（小さいもの）

- **ブレイクポイントの説明文**（タダシさん指示）: タイトル下は「**ダメージが変わる境目をチェック**」、GBL 1対1・個体値チェッカーからの遷移ボタンは「**この個体でダメージが変わる境目をチェック**」。「先回り」は使わない
- **ザシアン・ザマゼンタの「れきせんのゆうしゃ」は全ツールで名前に添える**: 個体値チェッカーは「ザシアン(れきせんのゆうしゃ)」「ザマゼンタ(れきせんのゆうしゃ)」（半角括弧の流儀）。他ツールは「ザシアン（れきせんのゆうしゃ）」
- **レイドシミュのタイムラインの「ボス残りHP」はGBL模擬戦のHPバーと同じ色**（50%超=緑／20%超=黄／20%以下=赤・同じ色コード）
- **図鑑のトレーナーバトルのSPアタック表に「効果」列**: 1行目「敵防⬇️」（対象 敵/自＋能力 攻/防＋段階ぶんの矢印・ほかの数字と同じ大きさ）、2行目に小さく発動確率（100%も書く）。攻防両方は「自攻⬆️防⬆️」、自分と相手の両方（ブロッキング）は並べる。
  **矢印が2本以上は字間を詰めて1行に**（タダシさんの指示が途中で切れていたための仮決め・別案なら差し替える）
- **「どのツールもスマホで見れない」の切り分け手順**: ①コードを疑う前に**画像URL（/assets/icons/home/icon-192.png）をSafariで直接開く**→出なければ回線 ②ホーム画面のアイコン（PWA）は**Safariとは別の保存領域**なので「Webサイトデータの削除」は効かない。アイコンの削除→追加し直しで初期化 ③2026-09-04の正体は**iPhoneの電波**（コードではなかった）

### 細かい表示の確定事項（2026-08-10）

- **ブレイクポイント**（表記は「ブレー**イ**クポイント」）は1行＝「**💥/🛡 わざ名 ／ ダメージの変化 ／ いま○○→必要○○ ／ 変化あり/変化なし**」の2段グリッド。
  与ダメは💥（⚔はバツに見えるため不採用）・被ダメは🛡。届かないときは「**5→5 変化なし**」（必要値は固定・いまの値は個体値で動く、の誤解を避けるラベル）
- **個体値ﾏﾆｭｱﾙ欄**はCPの下に**攻/防/HPの実数値**。**CP上限のないリーグ（マスター・ロケット団戦）では「入手別の1位個体」タブを隠す**
- **検索候補は「名前のすぐ横」にタイプアイコン（恒久ルール・2026-08-29確定・タダシさん指示）**: 全ツール共通。右端寄せ（space-between）は広い画面で離れて見えるので不可。
  候補行は `display:flex`＋`gap` の左詰め、種族値などの補助情報だけ `margin-left:auto` で右へ。個体値チェッカーは `typesHtml(p,16)`。
  ジム防衛は候補リストが無いので対象外・タイプ別火力は検索欄なし
- **ポケモン検索の候補は最大100件**（`searchPk`・`/iv-checker/` も）。前方一致を先に。**⚠ 候補を切るときは必ず件数を出すか100件まで見せる**（黙って切ると「データが無い」と読まれる。10件打ち切りでメガが一部しか無いと誤解された）
- **シールドを持たない側（したっぱのあいて・0枚設定）はシールドの行を出さない**（🛡0は残っているように見える）
- **特殊カップのタブは一覧を開いた時点で点灯**、選ばずに閉じたら実際のリーグの表示へ戻す

### メガシンカの追加SPアタック「＋わざ」（2026-08-31実装・2026-09-01にツール反映済み）

**メガシンカ中だけ使える3本目のSPアタック**（既存SP2本は残り、SPボタンが3つ並ぶ）。

**条件（公式ページ）**:
- **スーパーマックスレベル（メガLv4）に到達できるポケモンだけ**。当初13匹、**2026-09-08「チームメガ」からメガヘルガー・メガスピアーが加わり15匹**
- **メガシンカ中のみ**／**メガレベルは問わない**（上がるほど威力が上がる）／**条件を満たせば自動で覚える**（別枠）
- 使える: **レイド（メガ・スーパーメガ含む）／GOバトルリーグ**。使えない: **ジムに配置されたポケモンとのジムバトル**（公式「現時点では」）
- 開始: **2026-08-31 10:00（JST）**。カロス御三家メガの3本はGOフェス メガフィナーレ（9/5〜9/6）から

**メガレベルごとの威力倍率**（タダシさん確認済み・実装済み）:
**Lv1(ベース) 1.0 ／ Lv2(高レベル) 1.1 ／ Lv3(マックス) 1.2 ／ Lv4(スーパーマックス) 1.3**。

- **既定はLv4**。**「メガLv」タブ**で1〜4を切り替え
- 共通部品 **`assets/mega-lv.css` / `assets/mega-lv.js`**（自己完結）。保存キーは全ツール共通の **`site_megalv`**。
  `MegaLv.apply(moves, ids)` が**元の値を `_p0` に控えてから威力を書き換える**（二重に掛からない）
- **データ（godata・pvp_data）は素の値のまま**。倍率は読み込み直後の画面側だけで掛ける（自動更新での二重掛け防止）
- **⚠ セレクタは必ず `.megalv .mlvseg button`**。`.megalv button` だと `.modes button[aria-pressed="true"]` / `.gtabs button` に詳細度で負けて点灯色が消える
- **⚠ 威力は小数第1位で丸める**（185×1.3＝240.5）。ゲーム内の丸め方は未確認
- **⚠ 対戦（PvP）側にも同じ倍率**（公式がレイド限定と書いていないため）。効くのは図鑑の表示だけ
- **タブを出す条件**: **いまの一覧に「＋わざ」の行があるときだけ**。ただし**既定(Lv4)から変えているあいだは常に出す**（戻せなくなるのを防ぐ）。
  （火力チェッカー＝`rankHasPlus`／タイプ別火力＝`hasPlus`／レイドシミュ＝`usesPlus()`／図鑑＝その13匹のページ）。
  旧条件「メガ・ゲンシON」は廃止（押しても変わらず壊れて見えたため）
- **動く行を見せる**: メガLvの並びに「＋わざの威力だけ変わります」（`mega-lv.js` 内）／一覧の＋わざ名は**マゼンタ**（`.plusmv`・特別わざのオレンジと別色）
- **火力チェッカーの「自分のポケモン」は個体ごとにメガLvを持つ**（ランキング側＝全員同じレベルの物差しとは別）。
  `mine[i].mlv`（既定は登録時のランキング側の値）で持ち、`mineRows()` が `MegaLv.powerAt()` でその個体だけ差し替え（`D.moves` は書き換えない）。**＋わざを選んで登録したときだけ**出す
  - **⚠ PL上限の「メガLv4／メガLv4＋相棒／CPブースト」と連動**: 押したら個体のメガLvを4に、**メガLv3以下を選んだらPL上限のブーストを外す**（ありえない組み合わせを防ぐ）
  - 置き場所は**名前・個体値のすぐ下（PLの行の上）**、注記「＋わざの威力」。**PL上限ボタンの表記（メガLv4／メガLv4＋相棒）は全ツール統一の確定表記なので変えない**
- **⚠ 結果が変わるのは「＋わざが採用された行」だけ**。＋わざは**13本とも1ゲージ**なので「分割SP優遇」ON中は低レベルで分割あり技に替わることもある。
  **どちらも相手のタイプしだいで、ポケモン単位では決まらない**。基準: メガカイリューは対ディアルガで Lv1・Lv2 とも ばかぢから 38.30・Lv3から げきりん+／
  ドラゴン一覧では Lv1 から げきりん+（61.58→64.73→67.82→70.97）。**検証は実際の相手タイプで行う**
- **⚠ 火力の候補キャッシュのキーに必ずメガレベルを入れる**: `mpTopCache`（火力チェッカー）・`rankCache`（タイプ別火力）・`topCache`（レイドシミュ）。
  基準: ルギア★6の1位おまかせは Lv1=ネクロズマ(あかつきのつばさ)／Lv2以降=メガライチュウY

**13匹とわざの数値（タダシさんが実データと突き合わせて確定・2026-09-01）**:

| メガ | ＋わざ | 対戦: 威力/消費/能力変化 | レイド: 威力/全体/発生/ゲージ |
|---|---|---|---|
| メガミュウツーX | ばくれつパンチ+ | 130／80 | 130／2.5／1.0／-100 |
| メガミュウツーY | みらいよち+ | 130／80 | 140／2.5／1.2／-100 |
| メガライチュウX | ボルテッカー+ | 65／35（自分の防御-1・確定） | 170／3.5／2.1／-100 |
| メガライチュウY | でんじほう+ | 70／45（相手の攻撃-1・確定） | 160／3.5／2.8／-100 |
| メガウツボット | アシッドボム+ | 20／40（相手の防御-2・確定） | 160／3.0／2.1／-100 |
| メガスターミー | アクアブレイク+ | 55／40（相手の防御-1・30%） | 180／3.0／2.0／-100 |
| メガカイリュー | げきりん+ | 80／50 | 185／4.0／2.6／-100 |
| メガエアームド | ドリルくちばし+ | 60／35 | 170／2.5／1.9／-100 |
| メガブリガロン | タネばくだん+ | 60／40 | 150／2.0／1.1／-100 |
| メガマフォクシー | マジカルフレイム+ | 50／40（相手の攻撃-1・確定） | 140／2.0／1.3／-100 |
| メガゲッコウガ | なみのり+ | 55／35 | 130／1.5／1.2／-100 |
| メガタイレーツ | かわらわり+ | 40／35（相手の防御-1・確定） | 150／1.5／0.7／-100 |
| メガカラマネロ | サイケこうせん+ | 60／45 | 170／3.0／1.1／-100 |

- **全体の長さ・ダメージ発生は元わざと同じ**
- 対戦の数値は `build_pvp_data.py` の `MEGA_PLUS_PVP_CHECK` が毎回突き合わせ、ずれたら警告
- **⚠ 攻略サイトの「エネルギー」欄はPvPの消費ゲージの使い回し**。レイドのゲージ本数をそこから読まない
- **⚠ みらいよち+ の判定終了(damageWindowEnd)は暫定値**（全体の長さで代用。使うジム防衛はメガ対象外＝実害なし）

**⚠ 追加分: 公式発表の威力だけで先行収録（2026-09-04・タダシさん指示）**:
「時間は元わざを引き継ぐ」規則が13本で成立したため、**今後の＋わざは公式の威力だけで火力を計算してよい**。提供元に入る前に `MEGA_PLUS_MOVES` へ足す:

| メガ | ＋わざ | 対戦: 威力/消費/能力変化 | レイド: 威力/全体/発生/ゲージ |
|---|---|---|---|
| メガヘルガー | あくのはどう+ | 60／**40**（2026-09-22時点で提供元に収録済み） | 150／3.0／1.4／-100 |
| メガスピアー | とどめばり+ | 40／**35**（自分の攻撃+1・確定・同上） | 140／2.0／1.6／-100 |
| **メガムクホーク** | ブレイブバード+ | 70／**40**（自分の防御-3・確定・2026-09-16タダシさん確定） | 150／2.0／1.0／-100 |

- 時間は元わざの `godata.json` の値（あくのはどう d3.0/w1.4/we2.3・とどめばり d2.0/w1.6/we1.8・ブレイブバード d2.0/w1.0/we1.6）
- **メガムクホーク（2026-09-12追加・2026-09-19初登場・メガLv4対応）**: PvE側は `MANUAL_MEGA`（278/207/198・かくとう/ひこう）、
  対戦側は `MANUAL_RELEASED` に `staraptor_mega`。対戦の＋わざは **`MEGA_PLUS_PVP_MANUAL`**（`build_pvp_data.py`・提供元に無い＋わざの手動定義。
  提供元に入ればそちらが優先、見張り `(70, -40)`。消費未発表の間は `(70, None)` だった）から `plusMoves` へ収録。
  基準: ひこうのタイプ別火力で かぜおこし＋ブレイブバード+ 69.83が2位（Lv4）。**提供元に正式収録されたら `MANUAL_MEGA`／`MANUAL_RELEASED` の行は消してよい**
- **レイド側（火力チェッカー・タイプ別火力・/raid/・図鑑のジム・レイドタブ）には入る**
- **⚠ 対戦側（`pvp_data` の `plusMoves`）には消費ゲージ確定まで入れない**（トレーナーバトルの＋わざの消費は法則性なく変わる。威力だけだと実在しない性能になる）。
  `MEGA_PLUS_PVP_CHECK` に `(威力, None)` で控え、提供元に入れば自動収録・威力違いは警告。
  **`build_pvp_data.py` は plusMoves に無い＋わざにはポケモン側の `cp` の印を付けない**
- 基準: あくのタイプ別火力で メガヘルガー バークアウト＋あくのはどう+ が1位（Lv4）。ジム挑戦は `plus` の印で自動除外

**実装（2026-09-01）**:

| ツール | 反映 |
|---|---|
| **レイド火力チェッカー（ルート）** | **入る**＋メガLv切替（実測: 対ディアルガでメガミュウツーX＋ばくれつパンチ+ が1位。Lv1=74.30／Lv4=84.88） |
| **/raid/ レイドシミュ** | **入る**＋メガLv切替（実測: 対ディアルガ2人の実戦DPS Lv1=34.2／Lv4=39.1） |
| **/type-dps/ タイプ別火力** | **入る**＋メガLv切替（かくとう1位が同じくメガミュウツーX） |
| **/pokedex/ 図鑑** | **入る**（両タブ。紫の「追加」バッジ＋わざ詳細に説明。並べ替えに混ぜず表の先頭）＋メガLv切替 |
| **/gym-attack/ ジム挑戦** | **出さない**（ジムバトルで使えないため `build_gym.py` で除外） |
| **ジム防衛・マックスバトル** | 無関係（メガが対象外） |
| **/gbl/・/rocket/** | **2026-09-04に対応**（下の「GBL・ロケット団の＋わざとメガLv」の項）。**/breakpoint/・/battlelog/は未対応** |

- **`build_data.py` の `MEGA_PLUS_MOVES`** が唯一の定義元（わざID → メガのkey・タイプ・威力・時間）。
  注入は `SUPP_MOVES` と同じ仕組み、和名は `MEGA_PLUS_JA` → `JP_MOVE_FIX`。提供元に収録されたら自動取得値が優先
- **⚠ メガのわざリストは元ポケモンと同じ実体を共有**（`final[mega]['c'] is final[base]['c']`）。直接appendすると元にも増えるので `list(...)+[mid]` でコピーしてから足す。
  紐付けは新メガのPvPokeフォールバックより**後**
- godataのポケモンに **`plus`**（追加SPアタックのID）。バッジとジム挑戦の除外に使う
- **対戦データは `moves` に入れず `plusMoves` の別枠**、ポケモン側は **`cp`**。
  **⚠ /gbl/ のわざ欄は `D.moves` を全部なめて「その他のわざ」を作る**ため。既存の `c`/`ec` は触らない（GBL系の結果は不変）
- **図鑑だけ `Object.assign(PVP.moves, PVP.plusMoves)` で合流**。GBL側を3本目対応にするときはこの別枠を `moves` へ統合するところから
- 図鑑のわざ検索は**わざ名の側も `toKata` してから照合**（入力がカタカナに直されるため。ひらがな始まりのわざが引けなかった）

### その他の確定ルール
- **スーパーメガレイドのシールド補正**: シールド展開中のボス攻撃は**1.8倍**（火力チェッカー「🛡シールド破壊」タブの1発耐え判定）。
  2026-08-28に実測で確定: 個体値100%メガリザードンY PL50（CP5037・HP168・防御190.75）がメガエアームドのゴッドバード1発で約61%減＝ダメージ100〜105→補正1.73〜1.83（天候ブーストなし・タダシさん確認）。
  予測 1.8倍=残り38.1% / 1.9倍=35.1% / 2.0倍=31.5%。旧「2倍」は粗いバー読みだったので廃止。
  **バー読みで倍率を出すときはCPから個体を一意に特定し、ダメージの整数値で判別する**
- **スーパーメガレイドのゲージ50スタート**(2026-08-17・タダシさん提供): **ボスのシールド展開中にメガを場に出すと最初からゲージ50**。
  分割SP持ちはすぐ撃ってシールドを壊せる＝シールド破壊タブが分割SP持ちに絞る理由。火力チェッカーの注記(💡説明モード限定)と使い方タブに記載済み
- **ドラゴン7%ルール**: 1位が1ゲージ技なら分割あり技との火力差7%未満で分割ありを優先。
  **/dps/ と /type-dps/ は「分割SP優遇」タブでON/OFF・既定OFF**（下の専用セクション）。**ジム挑戦(/gym-attack/)は常時ON**（ツールの前提なのでタブにしない）。マックスバトルは5%(別仕様)
- **⚠️極低耐久マーク**: 耐久指数(防御種族値×HP種族値)が16000未満に表示。**シャドウ行は防御5/6の補正込みで判定**(該当最終進化: レパルダス・ヤミラミ)(2026-08-01確定)
- **めざめるパワー**: ノーマル/フェアリーは実在しない。16タイプに展開しボスごとに最適タイプで評価(`build_data.py`)
- **めざめるパワーの表示(2026-08-09確定)**: 火力チェッカーのランキングは「めざめるパワー」＋最適タイプのアイコン（同DPSなら全部）。タイプ名テキストは出さない（`template.html` の `moveLabel`/`bestCombo.hpTypes`）。自分の登録欄のわざ選択リストはタイプ名のまま
- **メガLv4(スーパーマックスレベル)**: メガはPL上限+2(52)、最高の相棒でさらに+1(最大53)。アメXL計算に影響させない。**表記は全ツール「メガLv4」「メガLv4＋相棒」**。/gbl/と/iv-checker/でメガ選択時のみ専用タブ(2026-08-02実装)
- **メガシンカCPブースト(イベントボーナス・2026-08-29実装・タダシさん指示)**: イベント中はメガLv4のPL+2が**PL+5(上限55)**。**最高の相棒を足しても55**(PL55以降のCPMが同じ)。
  **ボタン名は2行組み「CPブースト＋小さくイベントボーナス」**（個体値チェッカーのチップだけ「CPブースト(イベント)」）。
  /gbl/のタブは`word-break:keep-all`＋`<wbr>`で語の途中で折り返さない。メガLv4/メガLv4＋相棒と同じ並び(押すとPL55・再タップで解除)。
  **GBLの対戦計算にも効く**(基準: メガレックウザ SCP5426→5753)。実装先: **/gbl/・/rocket/**(`LEVELS_ALL`を55まで拡張)・
  **火力チェッカー**(`data-max="55"`)・**個体値チェッカー**(`data-cap="55"`・アメXLは表示PL−5で計算)。
  CPM: 52=0.8503/53=0.8553/54=0.8603/55=0.8653(半レベルも有り)。/breakpoint/のメガ4(52/53)と/pokedex/のメガLv4表示は未対応(必要ならタダシさんに確認)
- **わざ表記**: 全ツールで「ノーマルアタック」「SPアタック」
- **ギルガルド専用ノーマルアタック**: データ上「サイコカッター（独自性能）」「エアスラッシュ（独自性能）」（`build_pvp_data.py`）。チップ表示（`mvChip`）では「（独自）」(2026-08-09確定)
- **/gbl/ タイムラインの見かたタブ**: 「全ターン」が左・既定、「山場のみ」が右(2026-08-09確定)
- **ゲンシ表記**: ゲンシカイオーガ・ゲンシグラードン（キー末尾`_PRIMAL`）のマークは「**ゲンシ**」(専用の赤→青)。2026-08-06確定
- **ポケモン名のシャドウは、入力欄以外すべて名前の左のマーク**（2026-08-13タダシさん指示・`shMark()`・`<i class="shadowmark nmk">`。名前が見切れるため）:
  環境一覧・対策さがし・パーティ診断の表と見出し・得意な場面・診断文・入れ替え候補と記録・1対1パネルの名前・★登録リスト・ロケット団の「＋自分のポケモン」・模擬戦のVSカードと交代の選択肢・対策トップ5。
  **入力欄(input)は「シャドウ○○」の文字のまま**。模擬戦HUDの「S○○」も従来どおり。
  **マークの色は紫（`#b06cff`）に固定**（`.shadowmark.nmk`。文字色継承なので白くなるため）
- **タイプアイコンはエンボス加工（全ツール共通・2026-08-30タダシさん指示）**: 上に光・下に陰・外に落ち影。実装は `assets/type-icons.js` の注入CSS1か所。
  **⚠ 光と陰は::after・落ち影はfilterで作り、box-shadowには触らない**（`.tbadge{box-shadow:var(--tbadge)}` の縁取りを壊すため）。
  **20px未満は `.sm`＝控えめ版**。ライトは落ち影を紺系に弱める
- **タイプアイコンのグリフ5種（2026-08-30タダシさん承認済み）**: ほのお=先を約20%伸ばした三叉の炎・色#EB8840→#F09C3E ／ かくとう=拳1.13倍 ／
  エスパー=目を廃止し**渦巻き**(4巻き・重心(10,13)基準で中央配置・1.15倍) ／ じめん=**大小2つの山**(高さアップ版) ／
  ドラゴン=**公式アイコンを再現**(左向きの頭・角・時計回りの胴・羽毛状の尻尾・目は反時計回り45度・1.5倍)。
  **公式風は記憶で描かず、公式画像をcanvasで取り込んで座標を採り、半透明で重ねて合わせる**。非対称図形は**重心で中央配置**
- **シャドウマーク**: シャドウを表すタブ・チップは**文字ではなく紫の炎マーク**（`assets/shadow-icon.css` の `<i class="shadowmark"></i>`・色は`currentColor`を継承）。全ツール共通で、新規実装も同様。形は確定版なので変更時はユーザーに確認する(2026-08-06確定)
- **交代マーク**: 交代を表すマークは「⇄」の文字ではなく**黄色い循環矢印の画像**（`assets/gbl.css` の `<i class="swapmark"></i>`・ユーザー支給デザインを再現したインラインSVG。JSでは定数 `SWAPMK`）。/gbl/・/rocket/ の交代マークが必要な場面はすべてこれを使う。形は確定版なので変更時はユーザーに確認する(2026-08-09確定)

### 交換できないポケモンは最低個体値10（恒久ルール・2026-08-23確定・タダシさん指示）

**交換できないポケモンはリサーチ・レイド産だけ＝最低個体値10**。**個体値を総当たりする処理はすべて下限10**（新機能でも）。

- **データ**: `build_pvp_data.py` が `pvp_data.js` に **`ivf: 10`**。判定は **幻（`mythical` タグ）＝交換不可**、
  ただし **交換できる幻 `TRADEABLE_MYTH`**（**メルタン・メルメタルだけ**。デオキシス・ダークライ・ゲノセクトは交換不可）を除き、
  **幻ではないが交換できない `UNTRADEABLE_EXTRA`**（ジガルデ）を足す。メガ・フォルム違いは元を引き継ぐ。
  2026-08-23時点33匹: 幻30（セレビィ・ジラーチ・ミュウ・デオキシス4・ダークライ・ゲノセクト5・ビクティニ・ケルディオ・メロエッタ・
  ディアンシー（メガ含む）・フーパ・ボルケニオン・マーシャドー・ゼラオラ・ザルード・シェイミ・モモワロウ）＋ジガルデ3フォルム
- **PLの下限**（タダシさん確認: **スペシャルリサーチ産＝PL15／レイド産だけ＝PL20／ジガルデ（セル）＝PL20**）。
  既定 `LEVEL_FLOOR_DEFAULT=15`、`LEVEL_FLOOR` で**デオキシス・ダークライ・ジガルデだけ20**（ゲノセクト・ゼラオラ・ボルケニオンは15）。
  `pvp_data` の **`lvf`**（個体値チェッカーは `lf`）。CP制限に収まる最大PLが下限未満の個体は除く。
  **スーパーに収まる個体が無いもの**（ジガルデ・デオキシス（ノーマル／スピード）・ダークライ・ケルディオ・メロエッタ・フーパ（ときはなたれし）・マーシャドー・ザルード・メガディアンシー）は、
  ブレイクポイントで「PL◯未満の個体が存在しないため、このリーグのCP制限に収まる個体がありません」（`rank1().none`）
- **⚠ シャドウのときだけ下限が違う（2026-09-15・タダシさん指示「どのツールでも」）**: **シャドウダークライは最低個体値6**（通常は10）。
  `build_pvp_data.py` の **`SHADOW_IV_FLOOR = {'darkrai': 6}`** → **`ivfs: 6`**（`ivf` とは別・シャドウがあるキーだけ）。
  **`ivFloorOf(key, shadow)`**（gbl-app.js／breakpoint）は shadow のときだけ `ivfs` を返す＝**下限を見るときは必ずシャドウかどうかも渡す**
  （`rank1` にも渡す。GBLは第5引数・ブレイクポイントは第3引数。忘れると10で計算）。
  個体値チェッカーは `build_iv.py` の **`us: 6`** で、`u:1` でも「シャドウレイド（最低6）」だけ残す（既定は10）。
  基準: ハイパーのシャドウダークライ1位個体 **7/15/15**（通常は 10/13/13）。
  **⚠ ローカル確認では古い `pvp_data.js` が残ることがある**（`fetch(url,{cache:'reload'})` で取り直す）
- **適用先**: ブレイクポイント（`ivTable`/`foeTable`/`rank1`/個体値バー/入力欄のクランプ）／GBL・ロケット団（`rank1`・入手別1位のボタンで下限未満を隠す・手入力のクランプ）／
  個体値チェッカー（`build_iv.py` が `u:1` を付け、野生・交換・シャドウレイドを出さず10固定。「CPから」でも野生(0)に寄せない）。
  **新しく個体値を総当たりする処理は必ず `ivFloorOf(key)` から始める**
- **対象の増減は報告**: `build_pvp_data.py` が前回の `pvp_data.json` と比べ、`changes.md` に「交換できないポケモン（最低個体値10）の変更」を追記（毎朝のIssue）。
  **交換可否の変更はタダシさんの指示で `TRADEABLE_MYTH`/`UNTRADEABLE_EXTRA` を直す**
- 火力チェッカー・ジム系・マックスバトル・レイド・タイプ別火力は個体値15固定で影響なし

### 特別・レガシーわざの印は「わざ名をオレンジ＋うしろに *」（2026-09-07確定・恒久ルール・タダシさん指示）

全ツール共通。色は **`--special`（ダーク `#ff9750` ／ ライト `#d1490a`）**。画面は記号だけ、意味は使い方タブと長押し説明（📖）。

- **対象**: レイド火力(`template.html`＋`dps/index.html`)のランキングとわざ選択欄／レイドシミュ(`/raid/`)のわざ選択欄／タイプ別火力／マックスバトル／ステータス図鑑。
  **わざ名を出す画面を新しく作るときも必ずこの形**
- **廃止した旧表記**: 「(特)」／色だけで記号なし（気づけない）／「特別/レガシー」の枠付きピル（スマホで割れる）／金の「*」
- **ジム挑戦・ジム防衛は対象外**（`build_gym.py`／`build_defense.py` が `q+eq`・`c+ec` を混ぜ、データに区別が無い。出すならデータ側から）
- **GBL・ロケット団・対戦記録・ブレイクポイントは印なし**（付けるかはタダシさんの指示待ち）
- ボタンの `textContent` を書き換えない（**「名前は固定・点灯だけが変わる」**の共通ルール。マックスバトルの該当行は削除済み）

### タイプの並びは全ツール共通（2026-09-12確定・恒久ルール・タダシさん指示）

**タイプを並べて出すところは、どのツールもタイプ別火力ランキングの並び**:
**ノーマル・くさ・ほのお・みず・でんき・こおり・いわ・ひこう・むし・エスパー・ゴースト・かくとう・じめん・どく・ドラゴン・はがね・あく・フェアリー**（ノーマルは先頭）。

- 並びは **`assets/type-icons.js` の `TYPE_ORDER`／`typeOrder(t)`／`sortTypes(配列, 取り出し関数)`** の1か所（日本語名・英語名どちらでも可）。**新しいタイプ一覧も必ず `sortTypes` を通す**
- **データ（godata・pvp_data の `types`、ジム挑戦・マックスバトルの番号）はゲーム内の並びのまま**（番号で引く所がある）。並べ替えは表示の直前だけ
- 倍率・割合で並べる一覧（レイド火力・マックスバトルの弱点・対戦記録の相性グラフ）は**高い順を保ち、同じ値の中を共通の並び**に
- 対象: レイド火力の弱点（template.html と dps）／図鑑のタイプ・弱点の絞り込みと弱点・耐性の一覧／GBLの自由設定わざのタイプ選択と対戦記録の相性グラフ／マックスバトルの弱点／ジム挑戦の反撃技タイプ。
  **ポケモン自身の2タイプ（くさ/どく など）の順は変えない**

### 表記ルール(恒久)
- 入手方法の個体値下限グループは全ツールで「**レイド/ふか/リワード**」表記に統一(「タスク」「タマゴ」とは書かない)。今後の新実装も同様。
- ページに表示される文言・HTML・JS・コメント・データファイルのmemo欄を含め、他者や他サイトの名前(PokeMiners、PvPoke、Game Master、みんポケ、ポケらく等)は今後一切記載しない。新規コードでも同様。
- 出典・準拠元に触れる必要がある場合は「ゲーム内公開データ」「外部攻略情報」等の中立表現を使う。
- 例外: ビルドスクリプト内のデータ取得用URLは動作に必要なため残してよい(ページには表示されない)。
- **「殴る」と書かない(2026-08-17確定・タダシさん指示)**: 「**攻撃した**」「**攻撃しに行く**」と書く。画面・title・JS/Pythonのコメントすべて
- **ポケモンを「子」と呼ばない(2026-08-13確定・タダシさん指示)**: 「この子/その子/どの子」ではなく**「このポケモン」「この1匹」「この枠」**。
  表示文言・コード・ドキュメントすべて・全ツール（例: 「この子だけ◯」→「この1匹だけ◯」／「その子が勝てる相手」→「そのポケモンが勝てる相手」）
- **分析は「こうすべき」ではなく「いまこうなっている」を出す(2026-08-13確定・タダシさん指示)**: **GBLに正解の型は無い**ので戦い方を指図しない。
  - 終盤型で揃える（シールドを早く尽きさせSP連打）／序盤型で揃える（シールドの使いどころを失わせノーマルで押し切る）／バランス型、どれも立派な戦術
  - **「かたよっています」「バランスを取りましょう」のような良し悪しの言葉を使わない**。見出しも**正解からのズレを測る言い方にしない**（「3匹の働き」→「**得意な場面**」）
  - 提案も**足りないものを埋める形にしない**。「序盤型を探す／終盤型を探す」は**いつでも両方出す**
  - **穴（3匹とも勝てない相手）だけは例外**（事実として不利なので数を出し入れ替え候補につなげてよい）
- **「ぱっと見でわかる」を最優先(2026-08-12確定)**: **「読めばわかる」は当たり前**。全ツールの新規実装・改善に適用
  - **判断基準**: **タダシさん本人がパッと見で分からないなら、中級者以下には絶対に分からない**
  - 読みやすさは文字の量・色・字体・大きさ・余白で決まる。**説明文を足すのは最後の手段**、**削って際立たせる**を先に
  - **数字を並べるより状況を言葉にする**（「1匹 19」→「1匹頼み 19」）。**次に何をすればいいかまで出す**
  - **シミュレーターとしての性能が一番大切**。ただし**分かりづらくて使われなければ意味がない**
  - 失敗例: パーティ診断の円グラフ＋「3匹 10／1匹 19」（匹数が2つ並んで読めない）→ マスの図＋診断文
- **スマホの入力欄は文字16px以上(2026-08-13確定)**: iOSは16px未満の入力欄で自動拡大して戻らない。540px以下では入力欄を16pxに。全ツール・新規も同様
- **入力欄の中身を「打っている最中」に書き換えない(2026-08-21確定)**:
  **⚠ 日本語キーボードでは数字も変換中の文字**なので、`input` のたびに `value` を書き換えると1文字も打てない（PCでは再現しない）。
  - **`e.isComposing` のあいだは何もしない**。整えるのは **`compositionend`**
  - **全角数字は消さずに半角へ直す**（`[０-９]`→`[0-9]`）
  - 名前検索の欄（`#q`）と同じ形。**新しい入力欄も必ずこの形**
- **UI**: 見やすさ・わかりやすさに常に最善を尽くす(公開前にセルフレビュー必須)
- **文字数を極力減らす(2026-08-07確定)**: **絵やマークで表せるところは文字にしない**。全ツールの新規実装・修正・改善に適用
  - マークは意味が自明なものだけ(🛡シールド ／ ⏱時間 ／ ⇄交代 ／ ▶発動 ／ 🎲ランダム ／ 📌固定 ／ ⏸硬直 など)。推測できない記号より短い日本語
  - **説明・仕様の文章はページ最下部の「使い方」タブ1か所**(`#helptab`/`#helpbody`)。画面内には結論だけ
  - 数値は単位を削る(「27.0秒」→ ⏱27.0)。表の見出しもマーク
  - **画面から外した補足は消さずに `class="expl"` で隠す**（`.expl{display:none}` / **`.explain .expl{display:revert}`**。ルートに `explain` で一斉表示）。
    **⚠ `display:revert` にする・`block` 固定にしない**（行の途中の補足が改行される。`revert` なら `<span>` は行内・`<p>` はブロックのまま）
  - **説明ありモードの切り替えは実装済み（全7ページ）**。上の「説明ありモード(💡)は共通ファイル」のセクションを参照
- **UIの統一(恒久ルール)**: 複数のツールで同じ役割の表示・操作は**なるべく統一**。新しい画面を作る前に他ツールに同じ役割のUIがないか確認する
  - 例: 絞り込みタブは**ジム挑戦と同じ「独立ON/OFFのチップ」**（シャドウ・メガ・ゲンシは独立、両方ONなら両方入る）。「シールド破壊(メガ)」のような別ランキングに切り替わるタブだけ例外で、押している間は他の絞り込みを無効化

### 未実装ポケモンも図鑑・GBL・ロケット団・個体値チェッカーで選べる（2026-09-04実装・タダシさん指示）

**データ元のすべてのポケモンを実装前でも入力で使える**（動画用）。対象は**図鑑・GBL・ロケット団**（個体値チェッカーは `build_iv.py` が `r` を見ず元から全部入り）。
**火力チェッカー・タイプ別火力・レイド・ジム・マックスバトルには入れない**。

- `pvp_data` は `r:0` も収録済み。`gbl-app.js` は**検索候補だけ `KEYS_ALL`**（`r:0` 含む・`hid` 除く）を使い、名前の右に**オレンジの「未実装」札**（`.unrel`）。
  **環境一覧・対策さがしの「全ポケモン」・ロケット団ランキング・入れ替え候補は `KEYS`（実装済みだけ）**（ランキングが読めなくなるため）。
  選んだ未実装ポケモンは全枠で普通に計算できる（性能はデータ元のまま・変わることがある）
- **`hid:1`**: バトル中だけの内部フォルム（ギルガルド(ブレード)・ミミッキュ(ばけたすがた)・ウッウ(うのみ/まるのみ)・モルペコ(はらぺこ)）と重複エントリ（`duplicate` タグ）は候補に出さない。
  **データは残す**（`pvp-engine.js` が `aegislash_blade` を参照）。**⚠ キー名を `h` にしない**（`h` は**HP種族値**）
- **図鑑（`/pokedex/`）**: `buildDex` が `r:0` も載せ（`un:1`）、一覧・候補・ヒーローカードに「未実装」札。
  **ジム・レイド側の性能は `pokedex/unreleased.json`**（`build_data.py` が除外した未実装を書き出す・わざは `godata.json` の `moves` を共用）を図鑑だけが読んで `GO.pokemon` に合流（無くても動く）。
  内部フォルムは `hid` と同じ名前のものを載せない
- **アルセウス・シルヴァディの18フォルムなどもすべて出す**。メタモンは情報元が未実装扱いなので札が付く

### 新しいツールを作ったら専用アイコンとmanifestも必ず付ける（2026-08-20確定・タダシさん指摘）

**⚠ 新規ツールで抜けやすい**（`/type-dps/` と `/raid/` で抜け、ホーム画面で「ポ」の文字アイコンになった）。最初から4点を入れる:

1. **`assets/icons/<ツール名>/` に4サイズ**（`favicon-32.png` / `apple-touch-icon.png`(180) / `icon-192.png` / `icon-512.png`）
2. **`<ツール名>/manifest.webmanifest`**（`short_name` がホーム画面の名前）
3. **headに3行**（icon / apple-touch-icon / manifest）＋ **`<meta name="apple-mobile-web-app-title">`**
4. **濃い背景に、そのツールを表すシンボル1つ**（火力チェッカー=メーター／GBL=VS／ロケット団=R／マックスバトル=盾と剣／
   **タイプ別火力=虹色の棒グラフ**／**レイド=大きなボスに挑む3人**＝最低クリア人数の絵。ボスは**つり上がった目・への字の口・牙・角**）
   - 不採用: レイドの「★」（汎用的すぎる）
- **作り方**: SVGを書いて**ブラウザのcanvasでPNG化**（このMacに `rsvg-convert` も `node` も無い）。ローカルにPOSTで受け取るスクリプトで4サイズ書き出す
- 基準: くさ1位カミツルギ34.63（エクセル版の55.4は弱点1.6倍込み）

## 開発の基本

### データ更新パイプライン（2026-08-09に全ツール対応へ整備）

- **新ポケモン・新わざは必ず「全ツール」に反映する（恒久ルール・2026-08-13確定）**: 片方のツールだけの状態を残さない。
  **⚠ 対戦側(/gbl/・/rocket/)とPvE側(火力チェッカー・ジム挑戦・ジム防衛・マックスバトル)は提供元が別**で、自動ではそろわない
  - **1件直したら同種の漏れを必ず総点検する**: PvPokeの `gamemaster.json` と `godata.json` で**①わざ定義の有無 ②実装済みポケモンの習得わざの差分**を出し、入手可能かを外部攻略情報で1件ずつ確認
    （`legacyMoves`＝入手不可の旧レガシー技は**収録しない**／`eliteMoves`＝特別枠は収録／ドーブルは除外）
  - 直したら**火力チェッカー→ジム挑戦→ジム防衛→マックスバトルの順に再生成**し、画面で確認してからpush
- **毎日朝6時(JST)に `.github/workflows/update-data.yml` が全ツールを再生成し、変更があればそのまま反映（完全自動）。内容は「📢 ゲームデータの更新内容（8/13）新ポケモン1件」の形のIssueでメール通知**
  - 承認待ちPR（`peter-evans/create-pull-request`）はタダシさんの指示で廃止（毎日Mergeの手間）
  - 件名の要約は `build_data.py` の `HEADLINE:`（新ポケモン／新シャドウ／種族値変更／技の追加／新しい技／威力調整／削除の件数）
  - 前回のお知らせは自動で閉じる（件名が `📢 ゲームデータの更新内容` で始まるもの。`📢 環境リストの更新結果` とは互いに閉じ合わない）
  - **確認は事後**。おかしなデータはそのコミットを戻す
- 実行順（ローカルの手動更新も同じ）:
  1. `build_data.py` → `index.html`・`godata.json`（火力チェッカー。`changes.md`も生成）
  2. `gym-attack/build_gym.py` → `gym_data.json/js`（**godata.jsonから同期**）
  3. `gym-defense/build_defense.py` → `defense_data.js/json`
  4. `max-battle/build_max.py` → `max_data.js`
  5. `build_pvp_data.py` → `pvp_data.js/json`（/gbl/・/rocket/）
  6. `iv-checker/build_iv.py` → `iv-checker/index.html` の `const POKE`（**pvp_data.jsonから同期**）
- `gym_data.json` は手編集しない（同期で上書き）。表示名・並びは温存し、実質変化だけ更新する安全設計
- **わざの時間は3つ**: `godata.json` は **d=全体 / w=ダメージが出るまで / we=判定終了**（秒）、`gym_data.json` は **dur / dw / dwe**（ミリ秒）。
  元は `durationMs` / `damageWindowStartMs` / `damageWindowEndMs`。**ジム防衛のおすすめわざ選びが d と w の両方を使う**（じしん d=3.5・w=2.6）。
  `SUPP_MOVES` にも `damageWindowStartMs` / `damageWindowEndMs` を書く。**おんがえし だけ `damageWindowStartMs` が無く `damageWindowEndMs` で代用**
- **威力0のわざも収録**（はねる・へんしん・あくび）。**⚠ ポケモンが実際に覚えるものに限る**（外すと `VN_BM_001` 等の内部データを64件拾う）
- **新メガの取りこぼし防止**: `tempEvoOverrides`に載らない新メガは`build_data.py`がPvPoke側から自動補完。`MANUAL_MEGA`は最後の保険
- **⚠ 実装前（released=false）は自動補完に入らない**（`if not sp.get('released'): continue`）。**実装間近（タダシさんの指示）は `MANUAL_MEGA` に手で入れる**
  - 対戦側(`build_pvp_data.py`)は released を弾かないので**PvE側だけ取り残される**。**片方に入っていたら、もう片方を必ず確認**
  - わざは元ポケモンのものを引き継ぐ。**`fe:1`（最終進化）を必ず付ける**（火力チェッカーの絞り込みがずれる）
- **シャドウ実装が情報元に追いつかないときは `MANUAL_SHADOW` で先行反映**（2026-09-08新設）: 判定は**対戦データの `<key>_shadow` の有無**だけ。
  **`build_data.py`（大文字キー）と `build_pvp_data.py`（小文字キー）の両方に同じ顔ぶれを書く**。提供元に入っても害は無いので**消さずに残す**。
  登録: ゼクロム／カリキリ・ラランテス／スナバァ・シロデスナ／コソクムシ・グソクムシャ
- **⚠ シャドウを付けると `gym_data.json`・`max_data.js` の「わざの並び順」も最新にそろう**（顔ぶれは同じ）。ジム防衛のおすすめわざは同点時に**併記の1本目と2本目が入れ替わることがある**（中身は同じ）
- **ジム防衛（`/gym-defense/`）とマックスバトル（`/max-battle/`）にメガは入らない＝正しい**。新メガの展開対象は**火力チェッカー・ジム挑戦・/gbl/・/rocket/・個体値チェッカー**の5つ
- **Game Master提供元は2026年4〜8月に更新停止（8/29再開）、新わざの収録はまだ追いついていない**（プラズマフィスト・きょけんとつげき・ねらいうち・ダイビング・オーラぐるま・ダイマックスほうは9/2時点で未収録）。
  **⚠ 新わざはわざ定義ごと自動では入らない**ので、`build_data.py` の **`SUPP_MOVES`**（`apply_gm_supp()`）と `SUPP_EC`/`SUPP_C`/`SUPP_EQ` で補完（収録されたら自動取得値が優先）。
  **PvE数値の裏取りは `https://fight.pokebattler.com/moves`**。**⚠ 攻略サイトの表示値はタイプ一致1.2倍込み・ゲージ欄がPvP値のことがある**。
  対戦側はPvPoke由来で影響なし。**入手不可の旧レガシー技**（ラッキーのサイケこうせん等）は収録しない
- **日本語名が取れない技（`◯◯_PLUS`等）は`build_pvp_data.py`が収録しない**（英語名のまま並ぶのを防ぐ）
- `max-battle/latest.json` はGame Masterのキャッシュ（18MB超）。gitignore済み・コミットしない

### シーズンのわざアップデート（3か月ごと・2026-09-02確定・タダシさん指示）

シーズンごとの大型わざ調整は、公式発表を **`season_moves.py`** に書き写して突き合わせる。

- **⚠ 公式が数字で出すのは威力だけ**（メーターは「されやすく／にくく」のみ）。**手でデータを書き換えない**（ゲージ据え置きで実在しない性能になる）。**提供元のデータが入るのを待ち、チェッカーで確かめる**
- **`season_moves.py` は書き換えない受け皿**: `SEASON`・`MOVE_CHANGES`・`NEW_LEARNS` の3表を `pvp_data.json` / `godata.json` と突き合わせ **✅反映済み／⏳未反映／⚠想定外** を出す。
  `build_pvp_data.py` の最後から呼ばれ、未反映は `changes.md`（毎朝のお知らせ）へ
- **⚠想定外＝わざIDの対応づけの誤り**（または反映済みなのに旧値）。表を書いたら**「旧」の値がいまのデータと全部一致する**ことを必ず確かめる
- **ノーマルアタックはPvEとPvPでIDが違う**（PvEは末尾 `_FAST`。`BITE` / `BITE_FAST`）。キーも別体系（`muk_alolan` / `MUK_ALOLA`）なので表は両方持つ
- **シーズン開始は日本時間10:00**。**`update-data.yml` に JST 11:00 の回を足してある**

**2026-09-09「黄昏の旅路」での実績（初回運用の記録）**:

- 対戦側（`pvp_data`）は当日朝の自動更新で追いついた
- **ジム・レイド側（`godata`）は提供元が止まっていたので手動補完**: **`SUPP_C_2609`／`SUPP_Q_2609`**（新規習得42件＝`SUPP_C`/`SUPP_Q` へ合流・重複は足さない）と
  **`MOVE_FIX`**（サイコブースト 威力70→130・ゲージ2本→3本。**`when` の値が取得値と一致するときだけ上書き**＝提供元が更新したら自動で効かなくなる）。メガは元ポケモンへ足せば付く
- **⚠ 対戦側は本体キー（`toxtricity`）にしか新わざを足さず、フォルム違い（amped/low_key）は取り残す** → `build_pvp_data.py` の **`MANUAL_LEARN`**。`season_moves.py` は本体キーで見る
- **⚠ 新ポケモン実装の週は提供元の習得わざが誤っていることがある**: マフィティフ・オラチフは**「サイコファング」**（しっぺがえしではない）。`FORM_MOVE_FIX` で外し `SUPP_C_2609` で足した
- **環境上位リスト（`meta_lists.js`）は翌週月曜まで前シーズンのまま**。性能が変わったわざを使うポケモンは**確定値（`answer_key.js`）の見直しが要る**（タダシさんの判断）

**⚠ 反映漏れを絶対に出さない（2026-09-10タダシさん指示・恒久ルール）**:
**⚠ 環境上位は確定構成（answer_key.js）で戦うので、データを直しても新わざ・性能変更は使われない**（GBLでは致命的）。

- `season_moves.py` の `review_confirmed()` が**確定構成の見直し候補**（新しく覚えた／性能が変わったわざを使う行）を毎回出し `changes.md` にも追記。**シーズンが切り替わったら必ずタダシさんに見せて判断を仰ぐ**
- 2026-09の見直しは完了。**マスターのコノヨザル（通常/シャドウ）はタダシさん指示でけたぐりのまま**・メガミュウツーY（カップ専用）は据え置き。
  **次回も「情報元の今の推奨と同じ／違う」に仕分けて、違うものだけ見せる**

**次のシーズンでの手順**:

1. 公式ニュースの「わざの変更」「覚えられるわざの変更」を読む（**攻略サイトのまとめは誤りが混じるので公式の本文だけを写す**）
2. `season_moves.py` の3つの表を丸ごと書き替える（前シーズンぶんは消してよい）
3. `python3 season_moves.py` で「旧」の値が全部一致することを確かめる（⚠が出たら対応づけを直す）
4. 開始後に全項目が ✅ になるのを確かめる。**数日たっても⏳が残るなら提供元の遅れ**としてタダシさんへ報告

### その他

- `assets/meta_lists.js`(GBL環境上位リスト)は毎週月曜朝9時(JST)にGitHub Actionsが自動更新して直接push(`.github/workflows/update-meta.yml`→`build_meta.py`)。手編集しない。**push前に `git pull --rebase`**
- ルートの `index.html` は生成物。必ず `template.html` を編集し、`godata.json` と合わせて再生成して同期させる
- `/gbl/` のエンジン(`assets/pvp-engine.js`)は**2026年6月刷新の新バトルシステム準拠**(ダメージはターン末解決・1ターン技同時はタイ・SPは次ターン開始・**SPの解決と能力変化は、同じターンに着弾するノーマルより先**〈タダシさんの実機検証で確定〉・SPで倒された側のノーマルはそのターンには入らない＝相打ちにならない)。**⚠ 同じターンの順番は2026-09-22にタダシさんが確定し直した**（SPを入力したターンに当たるノーマルが先／そのノーマルで倒れてしまうならSPが先でそのノーマルは入らない／打ちかけの途中はSPが先。`.claude/rules/gbl.md` の同名の節が正）。ターン仕様は必ず2026年6月以降の情報を使う
- `/gbl/` のエンジンを変更したら `pvp-tests/engine-test.html` の全ケース緑を確認(ローカル専用)。**確認はキャッシュ無効のローカルサーバー(no-store)で**
- **Service Worker(4本立て)**: `sw.js`(`site-`) / `gbl/sw.js`(`gbl-`) / `rocket/sw.js`(`rkt-`) / `iv-checker/sw.js`(`ivc-`)。**ネットワーク優先＋`cache:'reload'`**で公開先の`max-age=600`を迂回。オフライン時のみキャッシュ
  - キャッシュ削除は**自分のプレフィックスだけ**(他ツールのオフライン用データを壊すため)
  - **⚠ ページを開くとき(`e.request.mode === 'navigate'`)の fetch は `redirect:'manual'`**（5本とも）。`/about`→`/about/` のような転送があると
    **Response served by service worker has redirections**（Chromeは ERR_FAILED）で表示を断られる
  - **旧アドレス（tarcy009.github.io）の古い仕組みは直せない**（1回目にエラーが出ることがあり、**再読み込みで開ける**）
  - `/gbl/`と`/rocket/`は起動に必要な一式を先読み。読み込むファイルを増やしたら**両方のSW**の`ASSETS`に追加し、`CACHE`のバージョン名を上げる
- `git push` とファイル削除は実行前に必ずユーザーへ確認する
